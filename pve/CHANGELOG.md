# Proxmox VE API Changelog

Endpoints introduced and changed per PVE release, derived from the commit history of `api-viewer/apidata.js` in [pve-docs](https://github.com/proxmox/pve-docs). 719 endpoints tracked across 98 commits; 864 change events. Generated by `tools/proxmox-api-parser/changelog-pveapi.js`.

## PVE 9.2 (2026-08-25)

### New endpoints (13)

- `DELETE /cluster/qemu/custom-cpu-models/{cputype}` — Delete a custom CPU model definition.
- `DELETE /nodes/{node}/ceph/fs/{name}` — Destroy a Ceph filesystem. Refuses if any PVE storage entry of type 'cephfs' still references the filesystem and is not disabled. Optionally also removes the storage entries and/or the underlying metadata and data pools.
- `GET /cluster/ceph/health-mute`
- `GET /cluster/qemu` — Cluster-wide QEMU index
- `GET /cluster/qemu/cpu-flags` — List of available CPU flags. Currently only implemented for x86_64, returns an empty list for aarch64.
- `GET /cluster/qemu/custom-cpu-models` — List all custom CPU model definitions visible to the user.
- `GET /cluster/qemu/custom-cpu-models/{cputype}` — Retrieve details about a specific custom CPU model.
- `GET /nodes/{node}/ceph/releases` — List all known Ceph releases, marking which ones can be installed on this node.
- `POST /cluster/ceph/restart-bulk` — Cluster-wide rolling restart of all Ceph daemons of the given type. For MON/MGR/MDS each daemon is restarted only after Ceph reports the previous one is back up and the next one is safe to stop. For OSDs the cluster path orchestrates the per-node endpoint at /nodes/{node}/ceph/restart-bulk on each node in turn, inheriting that endpoint's per-OSD 'noout' handling and resume support. The 'noout' flag itself is not exposed by this endpoint as it is OSD-specific (and for OSDs handled by the per-node sub-tasks).
- `POST /cluster/qemu/custom-cpu-models` — Add a custom CPU model definition.
- `POST /nodes/{node}/ceph/restart-bulk` — Rolling restart of all Ceph OSDs on this node. Each OSD is restarted only after Ceph reports the previous one is back up and the next one is safe to stop. For non-OSD Ceph daemons, use the cluster-wide endpoint at /cluster/ceph/restart-bulk. The 'noout' flag is applied only to the OSDs targeted by this run, so unrelated OSDs on other nodes that fail during the restart window still get out-marked normally. Aborting the resulting task (for example via 'pvesh task stop') triggers a SIGTERM handler that unsets the per-OSD 'noout' if this endpoint set it. Per-daemon progress is checkpointed in Ceph's config-key store ('pve/ceph-bulk-restart/node/<node>'), so an aborted run can be resumed by re-issuing this endpoint with 'resume=1'.
- `PUT /cluster/ceph/health-mute/{code}`
- `PUT /cluster/qemu/custom-cpu-models/{cputype}` — Update a custom CPU model definition.

### Changed endpoints (66)

- `GET /cluster/backup/{id}`: returns object: `-maxfiles`
- `GET /cluster/ha/resources/{sid}`: returns object: `+auto-rebalance`
- `GET /cluster/options`: returns object: `+allowed-tags`, `+bwlimit`, `+consent-text`, `+console`, `+crs`, `+description`, `+email_from`, `+fencing`, `+ha`, `+http_proxy`, `+keyboard`, `+language`, `+location`, `+mac_prefix`, `+max_workers`, `+migration`, `+migration_unsecure`, `+next-id`, `+notify`, `+registered-tags`, `+replication`, `+tag-style`, `+u2f`, `+user-tag-access`, `+webauthn`
- `GET /cluster/qemu/custom-cpu-models/{cputype}`: returns object: `+level`
- `GET /cluster/sdn/controllers/{controller}`: returns null: `+bgp-mode`, `+nodes`, `+peer-group-name`
- `GET /cluster/sdn/fabrics/fabric/{id}`: returns object: `+redistribute`
- `GET /cluster/sdn/zones/{zone}`: returns null: `+secondary-controllers`
- `GET /nodes/{node}/capabilities/qemu/cpu-flags`: added `accel`
- `GET /nodes/{node}/ceph/osd`: returns items object → none
- `GET /nodes/{node}/config`: returns object: `+location`
- `GET /nodes/{node}/journal`: added `identifiers`, `kernel`, `priority`, `service`, `structured`, `unit`, `units`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `-maxfiles`
- `POST /access/domains`: added `audiences`
- `POST /cluster/backup`: removed `maxfiles`
- `POST /cluster/firewall/aliases`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/firewall/groups`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/firewall/groups/{group}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/firewall/ipset`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/firewall/ipset/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/firewall/rules`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/ha/resources`: added `auto-rebalance`
- `POST /cluster/ha/rules`: removed `affinity`, `comment`, `disable`, `nodes`, `resources`, `rule`, `strict`, `type`
- `POST /cluster/qemu/custom-cpu-models`: added `level`; changed `reported-model` (optional: true → false)
- `POST /cluster/sdn/controllers`: added `bgp-mode`, `nodes`, `peer-group-name`
- `POST /cluster/sdn/fabrics/fabric`: added `redistribute`
- `POST /cluster/sdn/vnets/{vnet}/firewall/rules`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /cluster/sdn/zones`: added `secondary-controllers`
- `POST /nodes/{node}/firewall/rules`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/lxc`: changed `mp[n]` (format: ~idmap), `rootfs` (format: ~idmap)
- `POST /nodes/{node}/lxc/{vmid}/firewall/aliases`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/lxc/{vmid}/firewall/ipset`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/lxc/{vmid}/firewall/rules`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/qemu`: changed `net[n]` (format: +host-tunnel)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `net[n]` (format: +host-tunnel)
- `POST /nodes/{node}/qemu/{vmid}/firewall/aliases`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/qemu/{vmid}/firewall/ipset`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/qemu/{vmid}/firewall/rules`: changed `comment` (format: null → "pve-fw-comment-spec")
- `POST /nodes/{node}/vzdump`: removed `maxfiles`
- `POST /storage`: changed `blocksize` (format: null → "pve-storage-zfs-blocksize")
- `PUT /access/domains/{realm}`: added `audiences`
- `PUT /cluster/backup/{id}`: removed `maxfiles`
- `PUT /cluster/firewall/aliases/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /cluster/firewall/groups/{group}/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /cluster/firewall/ipset/{name}/{cidr}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /cluster/firewall/rules/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /cluster/ha/resources/{sid}`: added `auto-rebalance`
- `PUT /cluster/ha/rules/{rule}`: removed `affinity`, `comment`, `delete`, `digest`, `disable`, `nodes`, `resources`, `rule`, `strict`, `type`
- `PUT /cluster/options`: changed `crs` (format: ~ha-auto-rebalance-margin ~ha-auto-rebalance-threshold); changed `location` (format: {"latitude":{"description":"The latit… → "pve-node-location")
- `PUT /cluster/qemu/custom-cpu-models/{cputype}`: added `level`
- `PUT /cluster/sdn/controllers/{controller}`: added `bgp-mode`, `nodes`, `peer-group-name`
- `PUT /cluster/sdn/fabrics/fabric/{id}`: added `redistribute`
- `PUT /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /cluster/sdn/zones/{zone}`: added `secondary-controllers`
- `PUT /nodes/{node}/config`: added `location`; changed `location` (format: {"latitude":{"description":"The latit… → "pve-node-location")
- `PUT /nodes/{node}/firewall/rules/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `mp[n]` (format: ~idmap), `rootfs` (format: ~idmap)
- `PUT /nodes/{node}/lxc/{vmid}/firewall/aliases/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}/{cidr}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `net[n]` (format: +host-tunnel)
- `PUT /nodes/{node}/qemu/{vmid}/firewall/aliases/{name}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}/{cidr}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: changed `comment` (format: null → "pve-fw-comment-spec")
- `PUT /storage/{storage}`: changed `blocksize` (format: null → "pve-storage-zfs-blocksize")

## PVE 9.1 (2026-05-15)

### New endpoints (21)

- `DELETE /cluster/sdn/prefix-lists/{id}` — Delete Prefix List
- `DELETE /cluster/sdn/prefix-lists/{id}/entries/{url_seq}` — Delete Prefix List Entry
- `DELETE /cluster/sdn/route-maps/entries/{route-map-id}/entry/{order}` — Delete Route Map Entry
- `GET /cluster/sdn/dry-run` — Dry-run the SDN apply action and return the difference between the current configuration and the pending configuration
- `GET /cluster/sdn/prefix-lists` — List Prefix Lists
- `GET /cluster/sdn/prefix-lists/{id}` — Get Prefix List
- `GET /cluster/sdn/prefix-lists/{id}/entries` — List Prefix List Entries
- `GET /cluster/sdn/prefix-lists/{id}/entries/{url_seq}` — Get Prefix List Entry
- `GET /cluster/sdn/route-maps` — List Route Maps
- `GET /cluster/sdn/route-maps/entries` — Lists all route map entries.
- `GET /cluster/sdn/route-maps/entries/{route-map-id}` — List all entries for a given Route Map
- `GET /cluster/sdn/route-maps/entries/{route-map-id}/entry/{order}` — Get Route Map Entry
- `GET /nodes/{node}/storage/{storage}/identity` — Return identity information for this storage instance.
- `POST /cluster/ha/status/arm-ha` — Request re-arming the HA stack after it was disarmed.
- `POST /cluster/ha/status/disarm-ha` — Request disarming the HA stack, releasing all watchdogs cluster-wide.
- `POST /cluster/sdn/prefix-lists` — Create Prefix List
- `POST /cluster/sdn/prefix-lists/{id}/entries` — Create Prefix List Entry
- `POST /cluster/sdn/route-maps/entries` — Create Route Map entry
- `PUT /cluster/sdn/prefix-lists/{id}` — Update Prefix List
- `PUT /cluster/sdn/prefix-lists/{id}/entries/{url_seq}` — Update Prefix List Entry
- `PUT /cluster/sdn/route-maps/entries/{route-map-id}/entry/{order}` — Update Route Map Entry

### Changed endpoints (69)

- `DELETE /cluster/sdn/controllers/{controller}`: changed `controller` (format: "pve-sdn-controller-id" → null)
- `DELETE /cluster/sdn/dns/{dns}`: changed `dns` (format: "pve-sdn-dns-id" → null)
- `DELETE /cluster/sdn/ipams/{ipam}`: changed `ipam` (format: "pve-sdn-ipam-id" → null)
- `DELETE /cluster/sdn/vnets/{vnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `DELETE /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `DELETE /cluster/sdn/vnets/{vnet}/ips`: changed `vnet` (format: "pve-sdn-vnet-id" → null), `zone` (format: "pve-sdn-zone-id" → null)
- `DELETE /cluster/sdn/vnets/{vnet}/subnets/{subnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `DELETE /cluster/sdn/zones/{zone}`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `GET /cluster/backup/{id}`: returns object: `+all`, `+bwlimit`, `+comment`, `+compress`, `+dumpdir`, `+enabled`, `+exclude`, `+exclude-path`, `+fleecing`, `+id`, `+ionice`, `+lockwait`, `+mailnotification`, `+mailto`, `+maxfiles`, `+mode`, `+next-run`, `+node`, `+notes-template`, `+notification-mode`, `+pbs-change-detection-mode`, `+performance`, `+pigz`, `+pool`, `+protected`, `+prune-backups`, `+quiet`, `+remove`, `+repeat-missed`, `+schedule`, `+script`, `+stdexcludes`, `+stop`, `+stopwait`, `+storage`, `+tmpdir`, `+vmid`, `+zstd`
- `GET /cluster/sdn/controllers/{controller}`: changed `controller` (format: "pve-sdn-controller-id" → null)
- `GET /cluster/sdn/dns/{dns}`: changed `dns` (format: "pve-sdn-dns-id" → null)
- `GET /cluster/sdn/fabrics/fabric/{id}`: returns object: `+persistent_keepalive`, `+route_filter`
- `GET /cluster/sdn/fabrics/node/{fabric_id}/{node_id}`: returns null: `+allowed_ips`, `+endpoint`, `+peers`, `+public_key`, `+role`
- `GET /cluster/sdn/ipams/{ipam}`: changed `ipam` (format: "pve-sdn-ipam-id" → null)
- `GET /cluster/sdn/ipams/{ipam}/status`: changed `ipam` (format: "pve-sdn-ipam-id" → null)
- `GET /cluster/sdn/vnets/{vnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/firewall`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/firewall/options`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/firewall/rules`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/subnets`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/vnets/{vnet}/subnets/{subnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /cluster/sdn/zones/{zone}`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `GET /nodes/{node}/capabilities/qemu/cpu`: added `arch`
- `GET /nodes/{node}/capabilities/qemu/cpu-flags`: added `arch`
- `GET /nodes/{node}/capabilities/qemu/machines`: added `arch`
- `GET /nodes/{node}/qemu/{vmid}/agent/file-read`: added `count`, `decode`, `offset`
- `GET /nodes/{node}/sdn/vnets/{vnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /nodes/{node}/sdn/vnets/{vnet}/mac-vrf`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `GET /nodes/{node}/sdn/zones/{zone}`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `GET /nodes/{node}/sdn/zones/{zone}/content`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `POST /access/vncticket`: added `port`
- `POST /cluster/bulk-action/guest/migrate`: added `max-workers`
- `POST /cluster/bulk-action/guest/shutdown`: added `max-workers`
- `POST /cluster/bulk-action/guest/start`: added `max-workers`
- `POST /cluster/bulk-action/guest/suspend`: added `max-workers`
- `POST /cluster/config`: added `token-coefficient`
- `POST /cluster/sdn/controllers`: added `route-map-in`, `route-map-out`; changed `controller` (format: "pve-sdn-controller-id" → null)
- `POST /cluster/sdn/dns`: changed `dns` (format: "pve-sdn-dns-id" → null)
- `POST /cluster/sdn/fabrics/fabric`: added `persistent_keepalive`, `route_filter`
- `POST /cluster/sdn/fabrics/node/{fabric_id}`: added `allowed_ips`, `endpoint`, `peers`, `public_key`, `role`
- `POST /cluster/sdn/ipams`: changed `ipam` (format: "pve-sdn-ipam-id" → null)
- `POST /cluster/sdn/vnets`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `POST /cluster/sdn/vnets/{vnet}/firewall/rules`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `POST /cluster/sdn/vnets/{vnet}/ips`: changed `vnet` (format: "pve-sdn-vnet-id" → null), `zone` (format: "pve-sdn-zone-id" → null)
- `POST /cluster/sdn/zones`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `POST /nodes/{node}/lxc`: changed `net[n]` (format: ~host-managed); changed `mp[n]` (format: +keepattrs); changed `mp[n]` (format: +idmap), `rootfs` (format: +idmap)
- `POST /nodes/{node}/lxc/{vmid}/vncproxy`: returns null: `+password`
- `POST /nodes/{node}/migrateall`: added `max-workers`
- `POST /nodes/{node}/qemu`: changed `efidisk0` (format: ~ms-cert), `vga` (format: ~clipboard); changed `agent` (format: ~freeze-fs-on-backup +freeze-fs +guest-fsfreeze), `efidisk0` (format: ~ms-cert)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `efidisk0` (format: ~ms-cert), `vga` (format: ~clipboard); changed `agent` (format: ~freeze-fs-on-backup +freeze-fs +guest-fsfreeze), `efidisk0` (format: ~ms-cert)
- `POST /nodes/{node}/startall`: added `max-workers`
- `POST /nodes/{node}/stopall`: added `max-workers`
- `POST /nodes/{node}/suspendall`: added `max-workers`
- `POST /nodes/{node}/vncshell`: returns null: `+password`
- `PUT /access/users/{userid}/token/{tokenid}`: added `regenerate`; returns object: `+full-tokenid`, `+value`
- `PUT /cluster/options`: changed `crs` (format: ~ha +ha-auto-rebalance +ha-auto-rebalance-hold-duration +ha-auto-rebalance-margin +ha-auto-rebalance-method +ha-auto-rebalance-threshold); added `location`
- `PUT /cluster/sdn/controllers/{controller}`: added `route-map-in`, `route-map-out`; changed `controller` (format: "pve-sdn-controller-id" → null)
- `PUT /cluster/sdn/dns/{dns}`: changed `dns` (format: "pve-sdn-dns-id" → null)
- `PUT /cluster/sdn/fabrics/fabric/{id}`: added `persistent_keepalive`, `route_filter`
- `PUT /cluster/sdn/fabrics/node/{fabric_id}/{node_id}`: added `allowed_ips`, `endpoint`, `peers`, `public_key`, `role`; changed `delete` (optional: true → false)
- `PUT /cluster/sdn/ipams/{ipam}`: changed `ipam` (format: "pve-sdn-ipam-id" → null)
- `PUT /cluster/sdn/vnets/{vnet}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `PUT /cluster/sdn/vnets/{vnet}/firewall/options`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `PUT /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}`: changed `vnet` (format: "pve-sdn-vnet-id" → null)
- `PUT /cluster/sdn/vnets/{vnet}/ips`: changed `vnet` (format: "pve-sdn-vnet-id" → null), `zone` (format: "pve-sdn-zone-id" → null)
- `PUT /cluster/sdn/zones/{zone}`: changed `zone` (format: "pve-sdn-zone-id" → null)
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `net[n]` (format: ~host-managed); changed `mp[n]` (format: +keepattrs); changed `mp[n]` (format: +idmap), `rootfs` (format: +idmap)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `efidisk0` (format: ~ms-cert), `vga` (format: ~clipboard); changed `agent` (format: ~freeze-fs-on-backup +freeze-fs +guest-fsfreeze), `efidisk0` (format: ~ms-cert)

## PVE 9.0 (2025-11-17)

### New endpoints (42)

- `DELETE /cluster/ha/rules/{rule}` — Delete HA rule.
- `DELETE /cluster/sdn/fabrics/fabric/{id}` — Add a fabric
- `DELETE /cluster/sdn/fabrics/node/{fabric_id}/{node_id}` — Add a node
- `DELETE /cluster/sdn/lock` — Release global lock for SDN configuration
- `GET /cluster/bulk-action` — List resource types.
- `GET /cluster/bulk-action/guest` — Bulk action index.
- `GET /cluster/ha/rules` — Get HA rules.
- `GET /cluster/ha/rules/{rule}` — Read HA rule.
- `GET /cluster/sdn/fabrics` — SDN Fabrics Index
- `GET /cluster/sdn/fabrics/all` — SDN Fabrics Index
- `GET /cluster/sdn/fabrics/fabric` — SDN Fabrics Index
- `GET /cluster/sdn/fabrics/fabric/{id}` — Update a fabric
- `GET /cluster/sdn/fabrics/node` — SDN Fabrics Index
- `GET /cluster/sdn/fabrics/node/{fabric_id}` — SDN Fabrics Index
- `GET /cluster/sdn/fabrics/node/{fabric_id}/{node_id}` — Get a node
- `GET /nodes/{node}/capabilities/qemu/cpu-flags` — List of available VM-specific CPU flags. Returns an empty list for 'aarch64' as no VM-specific flags are defined for it yet.
- `GET /nodes/{node}/capabilities/qemu/migration` — Get node-specific QEMU migration capabilities of the node. Requires the 'Sys.Audit' permission on '/nodes/<node>'.
- `GET /nodes/{node}/lxc/{vmid}/migrate` — Get preconditions for migration.
- `GET /nodes/{node}/query-oci-repo-tags` — List all tags for an OCI repository reference.
- `GET /nodes/{node}/sdn/fabrics/{fabric}` — Directory index for SDN fabric status.
- `GET /nodes/{node}/sdn/fabrics/{fabric}/interfaces` — Get all interfaces for a fabric.
- `GET /nodes/{node}/sdn/fabrics/{fabric}/neighbors` — Get all neighbors for a fabric.
- `GET /nodes/{node}/sdn/fabrics/{fabric}/routes` — Get all routes for a fabric.
- `GET /nodes/{node}/sdn/vnets/{vnet}`
- `GET /nodes/{node}/sdn/vnets/{vnet}/mac-vrf` — Get the MAC VRF for a VNet in an EVPN zone.
- `GET /nodes/{node}/sdn/zones/{zone}/bridges` — Get a list of all bridges (vnets) that are part of a zone, as well as the ports that are members of that bridge.
- `GET /nodes/{node}/sdn/zones/{zone}/ip-vrf` — Get the IP VRF of an EVPN zone.
- `POST /access/vncticket` — verify VNC authentication ticket.
- `POST /cluster/bulk-action/guest/migrate` — Bulk migrate all guests on the cluster.
- `POST /cluster/bulk-action/guest/shutdown` — Bulk shutdown all guests on the cluster.
- `POST /cluster/bulk-action/guest/start` — Bulk start or resume all guests on the cluster.
- `POST /cluster/bulk-action/guest/suspend` — Bulk suspend all guests on the cluster.
- `POST /cluster/ha/rules` — Create HA rule.
- `POST /cluster/sdn/fabrics/fabric` — Add a fabric
- `POST /cluster/sdn/fabrics/node/{fabric_id}` — Add a node
- `POST /cluster/sdn/lock` — Acquire global lock for SDN configuration
- `POST /cluster/sdn/rollback` — Rollback pending changes to SDN configuration
- `POST /nodes/{node}/qemu/{vmid}/dbus-vmstate` — Control the dbus-vmstate helper for a given running VM.
- `POST /nodes/{node}/storage/{storage}/oci-registry-pull` — Pull an OCI image from a registry.
- `PUT /cluster/ha/rules/{rule}` — Update HA rule.
- `PUT /cluster/sdn/fabrics/fabric/{id}` — Update a fabric
- `PUT /cluster/sdn/fabrics/node/{fabric_id}/{node_id}` — Update a node

### Changed endpoints (70)

- `DELETE /cluster/ha/resources/{sid}`: added `purge`
- `DELETE /cluster/sdn/controllers/{controller}`: added `lock-token`
- `DELETE /cluster/sdn/dns/{dns}`: added `lock-token`
- `DELETE /cluster/sdn/ipams/{ipam}`: added `lock-token`
- `DELETE /cluster/sdn/vnets/{vnet}`: added `lock-token`
- `DELETE /cluster/sdn/vnets/{vnet}/subnets/{subnet}`: added `lock-token`
- `DELETE /cluster/sdn/zones/{zone}`: added `lock-token`
- `GET /access/roles/{roleid}`: returns object: `+VM.GuestAgent.Audit`, `+VM.GuestAgent.FileRead`, `+VM.GuestAgent.FileSystemMgmt`, `+VM.GuestAgent.FileWrite`, `+VM.GuestAgent.Unrestricted`, `-VM.Monitor`; returns object: `+VM.Replicate`
- `GET /cluster/acme/plugins/{id}`: returns object: `+api`, `+data`, `+digest`, `+disable`, `+nodes`, `+plugin`, `+type`, `+validation-delay`
- `GET /cluster/ha/resources/{sid}`: returns object: `+failback`
- `GET /cluster/metrics/export`: added `node-list`
- `GET /cluster/replication/{id}`: returns object: `+comment`, `+digest`, `+disable`, `+guest`, `+id`, `+jobnum`, `+rate`, `+remove_job`, `+schedule`, `+source`, `+target`, `+type`
- `GET /cluster/sdn/controllers/{controller}`: returns object → none
- `GET /cluster/sdn/fabrics/fabric/{id}`: returns object: `+lock-token`
- `GET /cluster/sdn/fabrics/node/{fabric_id}/{node_id}`: returns null: `+lock-token`
- `GET /cluster/sdn/vnets/{vnet}`: returns object → none
- `GET /cluster/sdn/zones/{zone}`: returns object → none
- `GET /nodes/{node}/capabilities/qemu/migration`: returns object: `+has-dbus-vmstate`, `-dbus-vmstate`
- `GET /nodes/{node}/lxc/{vmid}/config`: returns object: `+entrypoint`, `+env`
- `GET /nodes/{node}/lxc/{vmid}/migrate`: returns object: `+dependent-ha-resources`, `-comigrated-ha-resources`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+pressurecpusome`, `+pressureiofull`, `+pressureiosome`, `+pressurememoryfull`, `+pressurememorysome`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+running-nets-host-mtu`; returns object: `+allow-ksm`, `+intel-tdx`, `+meta`, `+parent`, `+runningcpu`, `+runningmachine`, `+snaptime`, `+vmstate`
- `GET /nodes/{node}/qemu/{vmid}/migrate`: returns object: `+has-dbus-vmstate`; returns object: `+comigrated-ha-resources`; returns object: `+dependent-ha-resources`, `-comigrated-ha-resources`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+memhost`, `+pressurecpufull`, `+pressurecpusome`, `+pressureiofull`, `+pressureiosome`, `+pressurememoryfull`, `+pressurememorysome`
- `GET /nodes/{node}/services/{service}/state`: returns object: `+active-state`, `+desc`, `+name`, `+service`, `+state`, `+unit-state`
- `GET /nodes/{node}/storage/{storage}/status`: returns object: `+active`, `+avail`, `+content`, `+enabled`, `+shared`, `+total`, `+type`, `+used`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `-notification-policy`, `-notification-target`
- `POST /cluster/backup`: removed `notification-policy`, `notification-target`
- `POST /cluster/ha/groups`: changed `nodes` (format: "pve-ha-group-node-list" → "pve-ha-node-list")
- `POST /cluster/ha/resources`: added `failback`
- `POST /cluster/ha/resources/{sid}/migrate`: returns null → object
- `POST /cluster/ha/resources/{sid}/relocate`: returns null → object
- `POST /cluster/ha/rules`: added `affinity`; changed `nodes` (format: "pve-ha-group-node-list" → "pve-ha-node-list")
- `POST /cluster/metrics/server/{id}`: added `otel-compression`, `otel-headers`, `otel-max-body-size`, `otel-path`, `otel-protocol`, `otel-resource-attributes`, `otel-timeout`, `otel-verify-ssl`
- `POST /cluster/sdn/controllers`: added `fabric`; added `lock-token`
- `POST /cluster/sdn/dns`: added `lock-token`
- `POST /cluster/sdn/fabrics/fabric`: added `lock-token`
- `POST /cluster/sdn/fabrics/node/{fabric_id}`: added `lock-token`
- `POST /cluster/sdn/ipams`: added `lock-token`
- `POST /cluster/sdn/vnets`: added `lock-token`
- `POST /cluster/sdn/vnets/{vnet}/subnets`: added `lock-token`
- `POST /cluster/sdn/zones`: added `fabric`; added `lock-token`
- `POST /nodes/{node}/lxc`: added `ha-managed`; changed `features` (format: ~nesting); added `entrypoint`, `env`; changed `net[n]` (format: +host-managed)
- `POST /nodes/{node}/qemu`: changed `virtiofs[n]` (format: -writeback); changed `ide[n]` (format: -cyls -heads -secs -trans), `sata[n]` (format: -cyls -heads -secs -trans), `scsi[n]` (format: -cyls -heads -secs -trans), `virtio[n]` (format: -cyls -heads -secs -trans); changed `net[n]` (format: ~mtu); added `running-nets-host-mtu`; added `allow-ksm`, `ha-managed`, `intel-tdx`; removed `running-nets-host-mtu`; changed `efidisk0` (format: +ms-cert), `machine` (format: +aw-bits), `tpmstate0` (format: +format)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `virtiofs[n]` (format: -writeback); changed `ide[n]` (format: -cyls -heads -secs -trans), `sata[n]` (format: -cyls -heads -secs -trans), `scsi[n]` (format: -cyls -heads -secs -trans), `virtio[n]` (format: -cyls -heads -secs -trans); changed `net[n]` (format: ~mtu); added `running-nets-host-mtu`; added `allow-ksm`, `intel-tdx`; removed `running-nets-host-mtu`; changed `efidisk0` (format: +ms-cert), `machine` (format: +aw-bits), `tpmstate0` (format: +format)
- `POST /nodes/{node}/qemu/{vmid}/migrate`: added `with-conntrack-state`
- `POST /nodes/{node}/qemu/{vmid}/status/start`: added `with-conntrack-state`; added `nets-host-mtu`; changed `machine` (format: +aw-bits)
- `POST /nodes/{node}/storage/{storage}/oci-registry-pull`: added `filename`
- `POST /nodes/{node}/vzdump`: removed `notification-policy`, `notification-target`
- `POST /storage`: removed `server2`, `transport`, `volume`; added `snapshot-as-volume-chain`, `zfs-base-path`; removed `maxfiles`; added `saferemove-stepsize`
- `PUT /access/users/{userid}/token/{tokenid}`: added `delete`
- `PUT /cluster/backup/{id}`: removed `notification-policy`, `notification-target`
- `PUT /cluster/ha/groups/{group}`: changed `nodes` (format: "pve-ha-group-node-list" → "pve-ha-node-list")
- `PUT /cluster/ha/resources/{sid}`: added `failback`
- `PUT /cluster/ha/rules/{rule}`: added `affinity`; changed `nodes` (format: "pve-ha-group-node-list" → "pve-ha-node-list")
- `PUT /cluster/metrics/server/{id}`: added `otel-compression`, `otel-headers`, `otel-max-body-size`, `otel-path`, `otel-protocol`, `otel-resource-attributes`, `otel-timeout`, `otel-verify-ssl`
- `PUT /cluster/options`: changed `migration` (format: ~network); added `replication`; changed `migration` (format: ~network)
- `PUT /cluster/sdn`: added `lock-token`, `release-lock`
- `PUT /cluster/sdn/controllers/{controller}`: added `fabric`; added `lock-token`
- `PUT /cluster/sdn/dns/{dns}`: added `lock-token`
- `PUT /cluster/sdn/fabrics/fabric/{id}`: added `lock-token`
- `PUT /cluster/sdn/fabrics/node/{fabric_id}/{node_id}`: added `lock-token`
- `PUT /cluster/sdn/ipams/{ipam}`: added `lock-token`
- `PUT /cluster/sdn/vnets/{vnet}`: added `lock-token`
- `PUT /cluster/sdn/vnets/{vnet}/subnets/{subnet}`: added `lock-token`
- `PUT /cluster/sdn/zones/{zone}`: added `fabric`; added `lock-token`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `features` (format: ~nesting); added `entrypoint`, `env`; changed `net[n]` (format: +host-managed)
- `PUT /nodes/{node}/network`: added `skip_frr`; added `regenerate-frr`; removed `skip_frr`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `virtiofs[n]` (format: -writeback); changed `ide[n]` (format: -cyls -heads -secs -trans), `sata[n]` (format: -cyls -heads -secs -trans), `scsi[n]` (format: -cyls -heads -secs -trans), `virtio[n]` (format: -cyls -heads -secs -trans); changed `net[n]` (format: ~mtu); added `running-nets-host-mtu`; added `allow-ksm`, `intel-tdx`; removed `running-nets-host-mtu`; changed `efidisk0` (format: +ms-cert), `machine` (format: +aw-bits), `tpmstate0` (format: +format)
- `PUT /storage/{storage}`: removed `server2`, `transport`; added `snapshot-as-volume-chain`, `zfs-base-path`; removed `maxfiles`; added `saferemove-stepsize`

## PVE 8.3 (2025-04-07)

### New endpoints (13)

- `DELETE /cluster/mapping/dir/{id}` — Remove directory mapping.
- `DELETE /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}` — Delete rule.
- `GET /cluster/mapping/dir` — List directory mapping
- `GET /cluster/mapping/dir/{id}` — Get directory mapping.
- `GET /cluster/sdn/vnets/{vnet}/firewall` — Directory index.
- `GET /cluster/sdn/vnets/{vnet}/firewall/options` — Get vnet firewall options.
- `GET /cluster/sdn/vnets/{vnet}/firewall/rules` — List rules.
- `GET /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}` — Get single rule data.
- `POST /cluster/mapping/dir` — Create a new directory mapping.
- `POST /cluster/sdn/vnets/{vnet}/firewall/rules` — Create new rule.
- `PUT /cluster/mapping/dir/{id}` — Update a directory mapping.
- `PUT /cluster/sdn/vnets/{vnet}/firewall/options` — Set Firewall options.
- `PUT /cluster/sdn/vnets/{vnet}/firewall/rules/{pos}` — Modify rule data.

### Changed endpoints (26)

- `GET /cluster/firewall/options`: returns object: `+policy_forward`
- `GET /nodes/{node}/config`: returns object: `+ballooning-target`
- `GET /nodes/{node}/firewall/options`: returns object: `+log_level_forward`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+cpu`, `+mem`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+virtiofs[n]`
- `GET /nodes/{node}/qemu/{vmid}/migrate`: returns object: `+mapped-resource-info`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+cpu`, `+mem`; returns object: `+serial`
- `GET /nodes/{node}/status`: returns object: `+cpu`, `+cpuinfo`, `+loadavg`, `+memory`, `+pveversion`, `+rootfs`
- `POST /access/domains`: added `groups-autocreate`, `groups-claim`, `groups-overwrite`, `query-userinfo`
- `POST /cluster/mapping/pci`: added `live-migration-capable`
- `POST /cluster/sdn/dns`: added `fingerprint`
- `POST /cluster/sdn/ipams`: added `fingerprint`
- `POST /nodes/{node}/qemu`: added `virtiofs[n]`; changed `efidisk0` (format: ~format), `ide[n]` (format: ~format), `machine` (format: +enable-s3 +enable-s4), `rng0` (format: {"max_bytes":{"default":1024,"descrip… → "pve-qm-rng"), `sata[n]` (format: ~format), `scsi[n]` (format: ~format), `virtio[n]` (format: ~format)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `virtiofs[n]`; changed `efidisk0` (format: ~format), `ide[n]` (format: ~format), `machine` (format: +enable-s3 +enable-s4), `rng0` (format: {"max_bytes":{"default":1024,"descrip… → "pve-qm-rng"), `sata[n]` (format: ~format), `scsi[n]` (format: ~format), `virtio[n]` (format: ~format)
- `POST /nodes/{node}/qemu/{vmid}/status/start`: changed `machine` (format: +enable-s3 +enable-s4)
- `POST /storage`: changed `format` (format: "pve-storage-format" → null)
- `PUT /access/domains/{realm}`: added `groups-autocreate`, `groups-claim`, `groups-overwrite`, `query-userinfo`
- `PUT /cluster/firewall/options`: added `policy_forward`
- `PUT /cluster/mapping/pci/{id}`: added `live-migration-capable`
- `PUT /cluster/options`: added `consent-text`
- `PUT /cluster/sdn/dns/{dns}`: added `fingerprint`
- `PUT /cluster/sdn/ipams/{ipam}`: added `fingerprint`
- `PUT /nodes/{node}/config`: added `ballooning-target`
- `PUT /nodes/{node}/firewall/options`: added `log_level_forward`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `virtiofs[n]`; changed `efidisk0` (format: ~format), `ide[n]` (format: ~format), `machine` (format: +enable-s3 +enable-s4), `rng0` (format: {"max_bytes":{"default":1024,"descrip… → "pve-qm-rng"), `sata[n]` (format: ~format), `scsi[n]` (format: ~format), `virtio[n]` (format: ~format)
- `PUT /storage/{storage}`: changed `format` (format: "pve-storage-format" → null)

## PVE 8.2 (2024-11-19)

### New endpoints (10)

- `DELETE /cluster/notifications/endpoints/webhook/{name}` — Remove webhook endpoint
- `GET /cluster/metrics/export` — Retrieve metrics of the cluster.
- `GET /cluster/notifications/endpoints/webhook` — Returns a list of all webhook endpoints
- `GET /cluster/notifications/endpoints/webhook/{name}` — Return a specific webhook endpoint
- `GET /cluster/notifications/matcher-field-values` — Returns known notification metadata fields and their known values
- `GET /cluster/notifications/matcher-fields` — Returns known notification metadata fields
- `GET /nodes/{node}/hardware/pci/{pci-id-or-mapping}` — Index of available pci methods
- `GET /nodes/{node}/hardware/pci/{pci-id-or-mapping}/mdev` — List mediated device types for given PCI device.
- `POST /cluster/notifications/endpoints/webhook` — Create a new webhook endpoint
- `PUT /cluster/notifications/endpoints/webhook/{name}` — Update existing webhook endpoint

### Changed endpoints (27)

- `GET /nodes/{node}/firewall/options`: returns object: `+nftables`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+disk`, `+diskread`, `+diskwrite`, `+netin`, `+netout`, `+template`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+amd-sev`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+diskread`, `+diskwrite`, `+netin`, `+netout`, `+template`
- `GET /nodes/{node}/subscription`: returns object: `+checktime`, `+key`, `+level`, `+message`, `+nextduedate`, `+productname`, `+regdate`, `+serverid`, `+signature`, `+sockets`, `+status`, `+url`
- `GET /nodes/{node}/tasks/{upid}/status`: returns object: `+pstart`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `+fleecing`; returns object: `+pbs-change-detection-mode`
- `POST /cluster/backup`: added `fleecing`; added `pbs-change-detection-mode`
- `POST /cluster/sdn/controllers`: changed `isis-net` (format: null → "pve-sdn-isis-net")
- `POST /cluster/sdn/vnets`: added `isolate-ports`
- `POST /nodes/{node}/lxc`: changed `dev[n]` (format: +deny-write), `mp[n]` (format: ~mountoptions), `rootfs` (format: ~mountoptions)
- `POST /nodes/{node}/lxc/{vmid}/status/stop`: added `overrule-shutdown`
- `POST /nodes/{node}/network`: added `bridge_vids`
- `POST /nodes/{node}/qemu`: changed `machine` (format: null → {"type":{"default_key":1,"description…); changed `tpmstate0` (format: ~version), `vga` (format: ~type); added `amd-sev`, `import-working-storage`; changed `net[n]` (format: ~macaddr), `usb[n]` (format: ~host)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `machine` (format: null → {"type":{"default_key":1,"description…); changed `tpmstate0` (format: ~version), `vga` (format: ~type); added `amd-sev`, `import-working-storage`; changed `net[n]` (format: ~macaddr), `usb[n]` (format: ~host)
- `POST /nodes/{node}/qemu/{vmid}/status/start`: changed `machine` (format: null → {"type":{"default_key":1,"description…)
- `POST /nodes/{node}/qemu/{vmid}/status/stop`: added `overrule-shutdown`
- `POST /nodes/{node}/vzdump`: added `fleecing`; added `job-id`, `pbs-change-detection-mode`; changed `job-id` (format: "pve-configid" → null)
- `PUT /cluster/backup/{id}`: added `fleecing`; added `pbs-change-detection-mode`
- `PUT /cluster/options`: changed `notify` (format: ~fencing ~package-updates ~replication ~target-fencing ~target-package-updates ~target-replication)
- `PUT /cluster/sdn/controllers/{controller}`: changed `isis-net` (format: null → "pve-sdn-isis-net")
- `PUT /cluster/sdn/vnets/{vnet}`: added `isolate-ports`
- `PUT /nodes/{node}/config`: changed `wakeonlan` (format: "mac-addr" → {"bind-interface":{"default":"The int…)
- `PUT /nodes/{node}/firewall/options`: added `nftables`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `dev[n]` (format: +deny-write), `mp[n]` (format: ~mountoptions), `rootfs` (format: ~mountoptions)
- `PUT /nodes/{node}/network/{iface}`: added `bridge_vids`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `machine` (format: null → {"type":{"default_key":1,"description…); changed `tpmstate0` (format: ~version), `vga` (format: ~type); added `amd-sev`; changed `net[n]` (format: ~macaddr), `usb[n]` (format: ~host)

## PVE 8.1 (2024-03-27)

### New endpoints (45)

- `DELETE /cluster/notifications/endpoints/gotify/{name}` — Remove gotify endpoint
- `DELETE /cluster/notifications/endpoints/sendmail/{name}` — Remove sendmail endpoint
- `DELETE /cluster/notifications/endpoints/smtp/{name}` — Remove smtp endpoint
- `DELETE /cluster/notifications/filters/{name}`
- `DELETE /cluster/notifications/groups/{name}`
- `DELETE /cluster/notifications/matchers/{name}` — Remove matcher
- `DELETE /cluster/sdn/vnets/{vnet}/ips` — Delete IP Mappings in a VNet
- `DELETE /pools` — Delete pool.
- `GET /cluster/acme/meta` — Retrieve ACME Directory Meta Information
- `GET /cluster/notifications` — Index for notification-related API endpoints.
- `GET /cluster/notifications/endpoints` — Index for all available endpoint types.
- `GET /cluster/notifications/endpoints/gotify` — Returns a list of all gotify endpoints
- `GET /cluster/notifications/endpoints/gotify/{name}` — Return a specific gotify endpoint
- `GET /cluster/notifications/endpoints/sendmail` — Returns a list of all sendmail endpoints
- `GET /cluster/notifications/endpoints/sendmail/{name}` — Return a specific sendmail endpoint
- `GET /cluster/notifications/endpoints/smtp` — Returns a list of all smtp endpoints
- `GET /cluster/notifications/endpoints/smtp/{name}` — Return a specific smtp endpoint
- `GET /cluster/notifications/filters`
- `GET /cluster/notifications/filters/{name}`
- `GET /cluster/notifications/groups`
- `GET /cluster/notifications/groups/{name}`
- `GET /cluster/notifications/matchers` — Returns a list of all matchers
- `GET /cluster/notifications/matchers/{name}` — Return a specific matcher
- `GET /cluster/notifications/targets` — Returns a list of all entities that can be used as notification targets.
- `GET /cluster/sdn/ipams/{ipam}/status` — List PVE IPAM Entries
- `GET /nodes/{node}/ceph/cfg/value` — Get configured values from either ceph.conf or the mon config DB. Underscores in section and key names are normalised to hyphens in the response, regardless of how they're written in the source.
- `GET /nodes/{node}/lxc/{vmid}/interfaces` — Get IP addresses of the specified container interface.
- `GET /nodes/{node}/storage/{storage}/import-metadata` — Get the base parameters for creating a guest which imports data from a foreign importable guest, like an ESXi VM
- `POST /cluster/notifications/endpoints/gotify` — Create a new gotify endpoint
- `POST /cluster/notifications/endpoints/sendmail` — Create a new sendmail endpoint
- `POST /cluster/notifications/endpoints/smtp` — Create a new smtp endpoint
- `POST /cluster/notifications/filters`
- `POST /cluster/notifications/groups`
- `POST /cluster/notifications/matchers` — Create a new matcher
- `POST /cluster/notifications/targets/{name}/test` — Send a test notification to a provided target.
- `POST /cluster/sdn/vnets/{vnet}/ips` — Create IP Mapping in a VNet
- `POST /nodes/{node}/suspendall` — Suspend all VMs.
- `PUT /cluster/notifications/endpoints/gotify/{name}` — Update existing gotify endpoint
- `PUT /cluster/notifications/endpoints/sendmail/{name}` — Update existing sendmail endpoint
- `PUT /cluster/notifications/endpoints/smtp/{name}` — Update existing smtp endpoint
- `PUT /cluster/notifications/filters/{name}`
- `PUT /cluster/notifications/groups/{name}`
- `PUT /cluster/notifications/matchers/{name}` — Update existing matcher
- `PUT /cluster/sdn/vnets/{vnet}/ips` — Update IP Mapping in a VNet
- `PUT /pools` — Update pool.

### Changed endpoints (38)

- `DELETE /cluster/sdn/vnets/{vnet}/ips`: added `ip`
- `GET /access/roles/{roleid}`: returns object: `+Sys.AccessNetwork`
- `GET /cluster/notifications/endpoints/gotify/{name}`: returns object: `+disable`, `-filter`
- `GET /cluster/notifications/endpoints/sendmail/{name}`: returns object: `+disable`, `-filter`
- `GET /nodes/{node}/lxc/{vmid}/config`: returns object: `+dev[n]`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+clipboard`
- `GET /nodes/{node}/status`: returns object: `+boot-info`, `+current-kernel`
- `GET /nodes/{node}/storage/{storage}/file-restore/download`: added `tar`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `+notification-policy`, `+notification-target`; returns object: `+notification-mode`
- `GET /pools`: added `poolid`, `type`
- `POST /access/domains`: added `check-connection`
- `POST /cluster/acme/account`: added `eab-hmac-key`, `eab-kid`
- `POST /cluster/backup`: added `notification-policy`, `notification-target`; added `notification-mode`
- `POST /cluster/notifications/endpoints/gotify`: added `disable`; removed `filter`
- `POST /cluster/notifications/endpoints/sendmail`: added `disable`; removed `filter`
- `POST /cluster/sdn/controllers`: added `isis-domain`, `isis-ifaces`, `isis-net`
- `POST /cluster/sdn/vnets/{vnet}/subnets`: added `dhcp-dns-server`, `dhcp-range`
- `POST /cluster/sdn/zones`: added `dhcp`
- `POST /nodes/{node}/ceph/osd`: added `osds-per-device`
- `POST /nodes/{node}/lxc`: added `dev[n]`
- `POST /nodes/{node}/qemu`: changed `efidisk0` (format: ~efitype), `memory` (type: "integer" → "string"; format: null → {"current":{"default":512,"default_ke…); changed `vga` (format: +clipboard); changed `scsi[n]` (format: +product +vendor), `vga` (format: ~clipboard)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `efidisk0` (format: ~efitype), `memory` (type: "integer" → "string"; format: null → {"current":{"default":512,"default_ke…); changed `vga` (format: +clipboard); changed `scsi[n]` (format: +product +vendor), `vga` (format: ~clipboard)
- `POST /nodes/{node}/storage/{storage}/download-url`: added `compression`
- `POST /nodes/{node}/vzdump`: added `notification-policy`, `notification-target`; added `notification-mode`
- `POST /storage`: added `skip-cert-verification`
- `PUT /access/domains/{realm}`: added `check-connection`
- `PUT /access/password`: added `confirmation-password`
- `PUT /cluster/backup/{id}`: added `notification-policy`, `notification-target`; added `notification-mode`
- `PUT /cluster/notifications/endpoints/gotify/{name}`: added `disable`; removed `filter`
- `PUT /cluster/notifications/endpoints/sendmail/{name}`: added `disable`; removed `filter`
- `PUT /cluster/options`: changed `notify` (format: ~package-updates +fencing +replication +target-fencing +target-package-updates +target-replication); changed `user-tag-access` (format: ~user-allow)
- `PUT /cluster/sdn/controllers/{controller}`: added `isis-domain`, `isis-ifaces`, `isis-net`
- `PUT /cluster/sdn/vnets/{vnet}/subnets/{subnet}`: added `dhcp-dns-server`, `dhcp-range`
- `PUT /cluster/sdn/zones/{zone}`: added `dhcp`
- `PUT /nodes/{node}/lxc/{vmid}/config`: added `dev[n]`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `efidisk0` (format: ~efitype), `memory` (type: "integer" → "string"; format: null → {"current":{"default":512,"default_ke…); changed `vga` (format: +clipboard); changed `scsi[n]` (format: +product +vendor), `vga` (format: ~clipboard)
- `PUT /pools/{poolid}`: added `allow-move`
- `PUT /storage/{storage}`: added `skip-cert-verification`

## PVE 8.0 (2023-06-19)

### New endpoints (17)

- `DELETE /cluster/jobs/realm-sync/{id}` — Delete realm-sync job definition.
- `DELETE /cluster/mapping/pci/{id}` — Remove Hardware Mapping.
- `DELETE /cluster/mapping/usb/{id}` — Remove Hardware Mapping.
- `GET /cluster/jobs/realm-sync` — List configured realm-sync-jobs.
- `GET /cluster/jobs/realm-sync/{id}` — Read realm-sync job definition.
- `GET /cluster/mapping` — List resource types.
- `GET /cluster/mapping/pci` — List PCI Hardware Mapping
- `GET /cluster/mapping/pci/{id}` — Get PCI Mapping.
- `GET /cluster/mapping/usb` — List USB Hardware Mappings
- `GET /cluster/mapping/usb/{id}` — Get USB Mapping.
- `POST /cluster/jobs/realm-sync/{id}` — Create new realm-sync job.
- `POST /cluster/mapping/pci` — Create a new hardware mapping.
- `POST /cluster/mapping/usb` — Create a new hardware mapping.
- `PUT /access/users/{userid}/unlock-tfa` — Unlock a user's TFA authentication.
- `PUT /cluster/jobs/realm-sync/{id}` — Update realm-sync job definition.
- `PUT /cluster/mapping/pci/{id}` — Update a hardware mapping.
- `PUT /cluster/mapping/usb/{id}` — Update a hardware mapping.

### Changed endpoints (16)

- `GET /access/roles/{roleid}`: returns object: `+Mapping.Audit`, `+Mapping.Modify`, `+Mapping.Use`, `+SDN.Use`
- `GET /cluster/ha/status/current`: returns items none → object
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+ciupgrade`
- `GET /nodes/{node}/qemu/{vmid}/migrate`: returns object: `+mapped-resources`
- `POST /cluster/backup`: changed `exclude-path` (type: "string" → "array"; format: "string-alist" → null)
- `POST /cluster/sdn/zones`: added `vxlan-port`
- `POST /nodes/{node}/qemu`: added `ciupgrade`; changed `usb[n]` (format: ~host +mapping)
- `POST /nodes/{node}/qemu/{vmid}/agent/exec`: changed `command` (type: "string" → "array"; optional: true → false; format: "string-alist" → null)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `ciupgrade`; changed `usb[n]` (format: ~host +mapping)
- `POST /nodes/{node}/vzdump`: changed `exclude-path` (type: "string" → "array"; format: "string-alist" → null)
- `POST /storage`: added `create-base-path`, `create-subdirs`
- `PUT /cluster/backup/{id}`: changed `exclude-path` (type: "string" → "array"; format: "string-alist" → null)
- `PUT /cluster/sdn/zones/{zone}`: added `vxlan-port`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `ciupgrade`; changed `usb[n]` (format: ~host +mapping)
- `PUT /nodes/{node}/qemu/{vmid}/resize`: returns null → string
- `PUT /storage/{storage}`: added `create-base-path`, `create-subdirs`

## PVE 7.4 (2023-03-20)

### New endpoints (12)

- `DELETE /nodes/{node}/ceph/pool/{name}` — Destroy pool
- `GET /nodes/{node}/ceph/cfg` — Directory index.
- `GET /nodes/{node}/ceph/cfg/db` — Get the Ceph configuration database.
- `GET /nodes/{node}/ceph/cfg/raw` — Get the Ceph configuration file.
- `GET /nodes/{node}/ceph/osd/{osdid}` — OSD index.
- `GET /nodes/{node}/ceph/osd/{osdid}/lv-info` — Get OSD volume details
- `GET /nodes/{node}/ceph/osd/{osdid}/metadata` — Get OSD details
- `GET /nodes/{node}/ceph/pool` — List all pools and their settings (which are settable by the POST/PUT endpoints).
- `GET /nodes/{node}/ceph/pool/{name}` — Pool index.
- `GET /nodes/{node}/ceph/pool/{name}/status` — Show the current pool status.
- `POST /nodes/{node}/ceph/pool` — Create Ceph pool
- `PUT /nodes/{node}/ceph/pool/{name}` — Change POOL settings

### Changed endpoints (17)

- `GET /cluster/ceph/metadata`: returns object: `+mds`, `+mgr`, `+mon`, `+node`, `+osd`
- `GET /nodes/{node}/ceph/osd`: returns items none → object
- `GET /nodes/{node}/firewall/log`: added `since`, `until`
- `GET /nodes/{node}/firewall/options`: returns object: `+nf_conntrack_helpers`
- `GET /nodes/{node}/lxc/{vmid}/firewall/log`: added `since`, `until`
- `GET /nodes/{node}/qemu/{vmid}/firewall/log`: added `since`, `until`
- `GET /nodes/{node}/tasks/{upid}/log`: added `download`
- `POST /nodes/{node}/lxc`: changed `net[n]` (format: +link_down)
- `POST /nodes/{node}/qemu`: changed `agent` (format: ~enabled +freeze-fs-on-backup), `efidisk0` (format: ~efitype)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `agent` (format: ~enabled +freeze-fs-on-backup), `efidisk0` (format: ~efitype)
- `POST /nodes/{node}/stopall`: added `force-stop`, `timeout`
- `POST /storage`: added `content-dirs`
- `PUT /cluster/options`: added `notify`; changed `crs` (format: ~ha +ha-rebalance-on-start), `user-tag-access` (format: ~user-allow)
- `PUT /nodes/{node}/firewall/options`: added `nf_conntrack_helpers`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `net[n]` (format: +link_down)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `agent` (format: ~enabled +freeze-fs-on-backup), `efidisk0` (format: ~efitype)
- `PUT /storage/{storage}`: added `content-dirs`

## PVE 7.3 (2022-11-21)

### Changed endpoints (1)

- `PUT /cluster/options`: changed `tag-style` (format: ~ordering +case-sensitive), `user-tag-access` (format: ~user-allow)

## PVE 7.2 (2022-11-18)

### New endpoints (9)

- `GET /nodes/{node}/ceph/cmd-safety` — Heuristical check if it is safe to perform an action.
- `GET /nodes/{node}/lxc/{vmid}/mtunnelwebsocket` — Migration tunnel endpoint for websocket upgrade - only for internal use by VM migration.
- `GET /nodes/{node}/qemu/{vmid}/cloudinit` — Get the cloudinit configuration with both current and pending values.
- `GET /nodes/{node}/qemu/{vmid}/mtunnelwebsocket` — Migration tunnel endpoint for websocket upgrade - only for internal use by VM migration.
- `POST /nodes/{node}/lxc/{vmid}/mtunnel` — Migration tunnel endpoint - only for internal use by CT migration.
- `POST /nodes/{node}/lxc/{vmid}/remote_migrate` — Migrate the container to another cluster. Creates a new migration task. EXPERIMENTAL feature!
- `POST /nodes/{node}/qemu/{vmid}/mtunnel` — Migration tunnel endpoint - only for internal use by VM migration.
- `POST /nodes/{node}/qemu/{vmid}/remote_migrate` — Migrate virtual machine to a remote cluster. Creates a new migration task. EXPERIMENTAL feature!
- `PUT /nodes/{node}/qemu/{vmid}/cloudinit` — Regenerate and change cloudinit config drive.

### Changed endpoints (20)

- `DELETE /cluster/firewall/ipset/{name}`: added `force`
- `DELETE /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}`: added `force`
- `DELETE /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}`: added `force`
- `GET /access/roles/{roleid}`: returns object: `+Sys.Incoming`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+affinity`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `+performance`
- `POST /cluster/backup`: added `repeat-missed`; added `performance`
- `POST /nodes/{node}/ceph/pools`: changed `erasure-coding` (format: ~failure-domain ~k)
- `POST /nodes/{node}/disks/zfs`: added `draid-config`
- `POST /nodes/{node}/execute`: changed `commands` (format: null → "pve-command-batch"); returns items none → object
- `POST /nodes/{node}/lxc`: changed `net[n]` (format: ~mtu)
- `POST /nodes/{node}/lxc/{vmid}/snapshot/{snapname}/rollback`: added `start`
- `POST /nodes/{node}/qemu`: added `affinity`; changed `usb[n]` (format: ~usb3); changed `net[n]` (format: ~queues)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `affinity`; changed `usb[n]` (format: ~usb3); changed `net[n]` (format: ~queues)
- `POST /nodes/{node}/qemu/{vmid}/snapshot/{snapname}/rollback`: added `start`
- `POST /nodes/{node}/vzdump`: added `performance`
- `PUT /cluster/backup/{id}`: added `repeat-missed`; added `performance`
- `PUT /cluster/options`: changed `next-id` (format: ~upper); changed `next-id` (format: ~upper); added `crs`, `registered-tags`, `tag-style`, `user-tag-access`; changed `webauthn` (format: ~id +allow-subdomains)
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `net[n]` (format: ~mtu)
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `affinity`; changed `usb[n]` (format: ~usb3); changed `net[n]` (format: ~queues)

## PVE 7.1 (2022-04-28)

### New endpoints (13)

- `DELETE /access/tfa/{userid}/{id}` — Delete a TFA entry by ID.
- `DELETE /nodes/{node}/disks/directory/{name}` — Unmounts the storage and removes the mount unit.
- `DELETE /nodes/{node}/disks/lvm/{name}` — Remove an LVM Volume Group.
- `DELETE /nodes/{node}/disks/lvmthin/{name}` — Remove an LVM thin pool.
- `DELETE /nodes/{node}/disks/zfs/{name}` — Destroy a ZFS pool.
- `GET /access/tfa` — List TFA configurations of users.
- `GET /access/tfa/{userid}` — List TFA configurations of users.
- `GET /access/tfa/{userid}/{id}` — Fetch a requested TFA entry if present.
- `GET /cluster/jobs` — Index for jobs related endpoints.
- `GET /cluster/jobs/schedule-analyze` — Returns a list of future schedule runtimes.
- `GET /nodes/{node}/query-url-metadata` — Query metadata of an URL: file size, file name and mime type.
- `POST /access/tfa/{userid}` — Add a TFA entry for a user.
- `PUT /access/tfa/{userid}/{id}` — Add a TFA entry for a user.

### Changed endpoints (40)

- `DELETE /nodes/{node}/ceph/pools/{name}`: added `remove_ecprofile`
- `GET /access/users/{userid}/tfa`: added `multiple`; returns object: `+types`
- `GET /nodes/{node}/config`: returns object: `+acme`, `+acmedomain[n]`, `+description`, `+digest`, `+startall-onboot-delay`, `+wakeonlan`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+tpmstate0`
- `GET /nodes/{node}/storage/{storage}/content/{volume}`: returns object: `+protected`
- `GET /nodes/{node}/tasks/{upid}/status`: returns object: `+exitstatus`, `+id`, `+node`, `+starttime`, `+type`, `+upid`, `+user`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `+notes-template`, `+protected`
- `GET /pools/{poolid}`: added `type`
- `GET /version`: returns object: `+console`
- `POST /access/domains`: added `acr-values`, `prompt`, `scopes`
- `POST /access/domains/{realm}/sync`: added `remove-vanished`
- `POST /access/ticket`: added `new-format`, `tfa-challenge`
- `POST /cluster/backup`: added `comment`, `id`, `schedule`; changed `starttime` (optional: false → true); added `notes-template`, `protected`
- `POST /cluster/metrics/server/{id}`: added `verify-certificate`
- `POST /cluster/sdn/controllers`: added `bgp-multipath-as-path-relax`
- `POST /cluster/sdn/zones`: added `advertise-subnets`, `disable-arp-nd-suppression`, `exitnodes-local-routing`; added `exitnodes-primary`, `rt-import`; added `bridge-disable-mac-learning`
- `POST /nodes/{node}/ceph/pools`: added `erasure-coding`
- `POST /nodes/{node}/lxc`: changed `mp[n]` (format: ~mountoptions), `rootfs` (format: ~mountoptions)
- `POST /nodes/{node}/lxc/{vmid}/migrate`: added `target-storage`
- `POST /nodes/{node}/lxc/{vmid}/move_volume`: added `target-digest`, `target-vmid`, `target-volume`; changed `storage` (optional: false → true)
- `POST /nodes/{node}/qemu`: added `tpmstate0`; changed `agent` (format: ~enabled), `efidisk0` (format: +efitype +pre-enrolled-keys), `net[n]` (format: ~model +e1000e), `rng0` (format: ~max_bytes ~source), `scsi[n]` (format: +ro), `virtio[n]` (format: +ro); changed `efidisk0` (format: +import-from), `ide[n]` (format: +import-from), `net[n]` (format: ~bridge), `sata[n]` (format: +import-from), `scsi[n]` (format: +import-from), `tpmstate0` (format: +import-from), `vga` (format: ~type), `virtio[n]` (format: +import-from)
- `POST /nodes/{node}/qemu/{vmid}/agent/file-write`: added `encode`
- `POST /nodes/{node}/qemu/{vmid}/config`: added `tpmstate0`; changed `agent` (format: ~enabled), `efidisk0` (format: +efitype +pre-enrolled-keys), `net[n]` (format: ~model +e1000e), `rng0` (format: ~max_bytes ~source), `scsi[n]` (format: +ro), `virtio[n]` (format: +ro); changed `efidisk0` (format: +import-from), `ide[n]` (format: +import-from), `net[n]` (format: ~bridge), `sata[n]` (format: +import-from), `scsi[n]` (format: +import-from), `tpmstate0` (format: +import-from), `vga` (format: ~type), `virtio[n]` (format: +import-from)
- `POST /nodes/{node}/qemu/{vmid}/migrate`: changed `targetstorage` (format: "storagepair-list" → "storage-pair-list")
- `POST /nodes/{node}/qemu/{vmid}/move_disk`: added `target-digest`, `target-disk`, `target-vmid`; changed `storage` (optional: false → true)
- `POST /nodes/{node}/qemu/{vmid}/status/start`: changed `targetstorage` (format: "storagepair-list" → "storage-pair-list")
- `POST /nodes/{node}/qemu/{vmid}/template`: returns null → string
- `POST /nodes/{node}/storage/{storage}/upload`: added `checksum`, `checksum-algorithm`
- `POST /nodes/{node}/vzdump`: added `notes-template`, `protected`
- `POST /storage`: added `fs-name`, `keyring`, `preallocation`; added `data-pool`; added `max-protected-backups`
- `PUT /access/domains/{realm}`: added `acr-values`, `prompt`, `scopes`
- `PUT /cluster/backup/{id}`: added `comment`, `schedule`; changed `starttime` (optional: false → true); added `notes-template`, `protected`
- `PUT /cluster/metrics/server/{id}`: added `verify-certificate`
- `PUT /cluster/options`: added `description`, `webauthn`; added `next-id`
- `PUT /cluster/sdn/controllers/{controller}`: added `bgp-multipath-as-path-relax`
- `PUT /cluster/sdn/zones/{zone}`: added `advertise-subnets`, `disable-arp-nd-suppression`, `exitnodes-local-routing`; added `exitnodes-primary`, `rt-import`; added `bridge-disable-mac-learning`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `mp[n]` (format: ~mountoptions), `rootfs` (format: ~mountoptions)
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `tpmstate0`; changed `agent` (format: ~enabled), `efidisk0` (format: +efitype +pre-enrolled-keys), `net[n]` (format: ~model +e1000e), `rng0` (format: ~max_bytes ~source), `scsi[n]` (format: +ro), `virtio[n]` (format: +ro); changed `efidisk0` (format: +import-from), `ide[n]` (format: +import-from), `net[n]` (format: ~bridge), `sata[n]` (format: +import-from), `scsi[n]` (format: +import-from), `tpmstate0` (format: +import-from), `vga` (format: ~type), `virtio[n]` (format: +import-from)
- `PUT /nodes/{node}/storage/{storage}/content/{volume}`: added `protected`
- `PUT /storage/{storage}`: added `fs-name`, `keyring`, `preallocation`; added `data-pool`; added `max-protected-backups`

## PVE 7.0 (2021-07-02)

### New endpoints (12)

- `GET /access/openid` — Directory index.
- `GET /cluster/backup-info` — Index for backup info related endpoints
- `GET /cluster/backup-info/not-backed-up` — Shows all guests which are not covered by any backup job.
- `GET /nodes/{node}/apt/repositories` — Get APT repository information.
- `GET /nodes/{node}/capabilities/qemu/cpu` — List all custom and default CPU models.
- `GET /nodes/{node}/vzdump/defaults` — Get the currently configured vzdump defaults.
- `POST /access/openid/auth-url` — Get the OpenId Authorization Url for the specified realm.
- `POST /access/openid/login` —  Verify OpenID authorization code and create a ticket.
- `POST /nodes/{node}/apt/repositories` — Change the properties of a repository. Currently only allows enabling/disabling.
- `POST /nodes/{node}/storage/{storage}/download-url` — Download templates, ISO images, OVAs and VM images by using an URL.
- `PUT /nodes/{node}/apt/repositories` — Add a standard repository to the configuration
- `PUT /nodes/{node}/disks/wipedisk` — Wipe a disk or partition.

### Changed endpoints (21)

- `GET /access/roles/{roleid}`: returns object: `+Pool.Audit`
- `GET /nodes/{node}/tasks`: added `since`, `statusfilter`, `until`
- `GET /nodes/{node}/vzdump/defaults`: returns object: `-size`
- `POST /access/domains`: added `autocreate`, `client-id`, `client-key`, `issuer-url`, `username-claim`
- `POST /cluster/backup`: removed `size`
- `POST /cluster/sdn/zones`: changed `ipam` (optional: false → true)
- `POST /nodes/{node}/ceph/mon/{monid}`: changed `mon-address` (format: "ip" → "ip-list")
- `POST /nodes/{node}/ceph/osd`: added `db_dev_size`, `wal_dev_size`; removed `db_size`, `wal_size`
- `POST /nodes/{node}/lxc/{vmid}/migrate`: removed `force`
- `POST /nodes/{node}/qemu`: changed `ide[n]` (format: ~aio), `sata[n]` (format: ~aio), `scsi[n]` (format: ~aio), `virtio[n]` (format: ~aio)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `ide[n]` (format: ~aio), `sata[n]` (format: ~aio), `scsi[n]` (format: ~aio), `virtio[n]` (format: ~aio)
- `POST /nodes/{node}/spiceshell`: removed `upgrade`
- `POST /nodes/{node}/termproxy`: removed `upgrade`
- `POST /nodes/{node}/vncshell`: removed `upgrade`
- `POST /nodes/{node}/vzdump`: removed `size`
- `POST /storage`: removed `redundancy`; added `nocow`
- `PUT /access/domains/{realm}`: added `autocreate`, `client-id`, `client-key`, `issuer-url`
- `PUT /cluster/backup/{id}`: removed `size`
- `PUT /cluster/sdn/zones/{zone}`: changed `ipam` (optional: false → true)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `ide[n]` (format: ~aio), `sata[n]` (format: ~aio), `scsi[n]` (format: ~aio), `virtio[n]` (format: ~aio)
- `PUT /storage/{storage}`: removed `redundancy`; added `nocow`

## PVE 6.4 (2021-04-26)

### New endpoints (23)

- `DELETE /cluster/sdn/dns/{dns}` — Delete sdn dns object configuration.
- `DELETE /cluster/sdn/ipams/{ipam}` — Delete sdn ipam object configuration.
- `DELETE /cluster/sdn/vnets/{vnet}/subnets/{subnet}` — Delete sdn subnet object configuration.
- `GET /cluster/sdn/dns` — SDN dns index.
- `GET /cluster/sdn/dns/{dns}` — Read sdn dns configuration.
- `GET /cluster/sdn/ipams` — SDN ipams index.
- `GET /cluster/sdn/ipams/{ipam}` — Read sdn ipam configuration.
- `GET /cluster/sdn/vnets/{vnet}/subnets` — SDN subnets index.
- `GET /cluster/sdn/vnets/{vnet}/subnets/{subnet}` — Read sdn subnet configuration.
- `GET /nodes/{node}/capabilities` — Node capabilities index.
- `GET /nodes/{node}/capabilities/qemu` — QEMU capabilities index.
- `GET /nodes/{node}/capabilities/qemu/machines` — Get available QEMU/KVM machine types.
- `GET /nodes/{node}/ceph/pools/{name}`
- `GET /nodes/{node}/hardware/usb` — List local USB devices.
- `GET /nodes/{node}/scan/pbs` — Scan remote Proxmox Backup Server.
- `GET /nodes/{node}/storage/{storage}/file-restore/download` — Extract a file or directory (as zip archive) from a PBS backup.
- `GET /nodes/{node}/storage/{storage}/file-restore/list` — List files and directories for single file restore under the given path.
- `POST /cluster/sdn/dns` — Create a new sdn dns object.
- `POST /cluster/sdn/ipams` — Create a new sdn ipam object.
- `POST /cluster/sdn/vnets/{vnet}/subnets` — Create a new sdn subnet object.
- `PUT /cluster/sdn/dns/{dns}` — Update sdn dns object configuration.
- `PUT /cluster/sdn/ipams/{ipam}` — Update sdn ipam object configuration.
- `PUT /cluster/sdn/vnets/{vnet}/subnets/{subnet}` — Update sdn subnet object configuration.

### Changed endpoints (30)

- `DELETE /nodes/{node}/lxc/{vmid}`: added `destroy-unreferenced-disks`
- `DELETE /nodes/{node}/qemu/{vmid}`: added `destroy-unreferenced-disks`
- `GET /cluster/sdn/controllers`: added `pending`, `running`
- `GET /cluster/sdn/controllers/{controller}`: added `pending`, `running`
- `GET /cluster/sdn/vnets`: added `pending`, `running`
- `GET /cluster/sdn/vnets/{vnet}`: added `pending`, `running`
- `GET /cluster/sdn/zones`: added `pending`, `running`
- `GET /cluster/sdn/zones/{zone}`: added `pending`, `running`
- `GET /nodes/{node}/disks/list`: added `include-partitions`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+running-machine`, `+running-qemu`
- `POST /cluster/backup`: changed `mailto` (format: "string-list" → "email-or-username-list")
- `POST /cluster/metrics/server/{id}`: added `api-path-prefix`, `bucket`, `influxdbproto`, `max-body-size`, `organization`, `token`
- `POST /cluster/sdn/controllers`: added `ebgp`, `ebgp-multihop`, `loopback`, `node`; removed `gateway-external-peers`, `gateway-nodes`
- `POST /cluster/sdn/vnets`: removed `ipv4`, `ipv6`, `mac`
- `POST /cluster/sdn/zones`: added `dns`, `dnszone`, `exitnodes`, `ipam`, `mac`, `reversedns`
- `POST /nodes/{node}/ceph/pools`: added `pg_num_min`, `target_size`, `target_size_ratio`
- `POST /nodes/{node}/lxc`: changed `nameserver` (format: "address-list" → "lxc-ip-with-ll-iface-list")
- `POST /nodes/{node}/qemu`: added `live-restore`; changed `agent` (format: ~fstrim_cloned_disks), `audio0` (format: ~driver)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `agent` (format: ~fstrim_cloned_disks), `audio0` (format: ~driver)
- `POST /nodes/{node}/vzdump`: changed `mailto` (format: "string-list" → "email-or-username-list")
- `POST /storage`: added `master-pubkey`, `namespace`
- `PUT /cluster/backup/{id}`: changed `mailto` (format: "string-list" → "email-or-username-list")
- `PUT /cluster/metrics/server/{id}`: added `api-path-prefix`, `bucket`, `influxdbproto`, `max-body-size`, `organization`, `token`
- `PUT /cluster/sdn/controllers/{controller}`: added `ebgp`, `ebgp-multihop`, `loopback`, `node`; removed `gateway-external-peers`, `gateway-nodes`
- `PUT /cluster/sdn/vnets/{vnet}`: removed `ipv4`, `ipv6`, `mac`
- `PUT /cluster/sdn/zones/{zone}`: added `dns`, `dnszone`, `exitnodes`, `ipam`, `mac`, `reversedns`
- `PUT /nodes/{node}/ceph/pools/{name}`: added `pg_num_min`, `target_size`, `target_size_ratio`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `nameserver` (format: "address-list" → "lxc-ip-with-ll-iface-list")
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `agent` (format: ~fstrim_cloned_disks), `audio0` (format: ~driver)
- `PUT /storage/{storage}`: added `master-pubkey`, `namespace`

## PVE 6.2 (2020-11-24)

### New endpoints (41)

- `DELETE /cluster/acme/plugins/{id}` — Delete ACME plugin configuration.
- `DELETE /cluster/metrics/server/{id}` — Remove Metric server.
- `DELETE /cluster/sdn/controllers/{controller}` — Delete sdn controller object configuration.
- `DELETE /cluster/sdn/vnets/{vnet}` — Delete sdn vnet object configuration.
- `DELETE /cluster/sdn/zones/{zone}` — Delete sdn zone object configuration.
- `DELETE /nodes/{node}/storage/{storage}/prunebackups` — Prune backups. Only those using the standard naming scheme are considered.
- `DELETE /nodes/{node}/subscription` — Delete subscription key of this node.
- `GET /cluster/acme/challenge-schema` — Get schema of ACME challenge types.
- `GET /cluster/acme/plugins` — ACME plugin index.
- `GET /cluster/acme/plugins/{id}` — Get ACME plugin configuration.
- `GET /cluster/backup/{id}/included_volumes` — Returns included guests and the backup status of their disks. Optimized to be used in ExtJS tree views.
- `GET /cluster/backupinfo`
- `GET /cluster/backupinfo/not_backed_up`
- `GET /cluster/config/apiversion` — Return the version of the cluster join API available on this node.
- `GET /cluster/metrics` — Metrics index.
- `GET /cluster/metrics/server` — List configured metric servers.
- `GET /cluster/metrics/server/{id}` — Read metric server configuration.
- `GET /cluster/sdn/controllers` — SDN controllers index.
- `GET /cluster/sdn/controllers/{controller}` — Read sdn controller configuration.
- `GET /cluster/sdn/vnets` — SDN vnets index.
- `GET /cluster/sdn/vnets/{vnet}` — Read sdn vnet configuration.
- `GET /cluster/sdn/zones` — SDN zones index.
- `GET /cluster/sdn/zones/{zone}` — Read sdn zone configuration.
- `GET /nodes/{node}/cpu`
- `GET /nodes/{node}/sdn/zones` — Get status for all zones.
- `GET /nodes/{node}/sdn/zones/{zone}` — Directory index for SDN zone status.
- `GET /nodes/{node}/sdn/zones/{zone}/content` — List zone content.
- `GET /nodes/{node}/storage/{storage}/prunebackups` — Get prune information for backups. NOTE: this is only a preview and might not be what a subsequent prune call does if backups are removed/added in the meantime.
- `POST /access/domains/{realm}/sync` — Syncs users and/or groups from the configured LDAP to user.cfg. NOTE: Synced groups will have the name 'name-$realm', so make sure those groups do not exist to prevent overwriting.
- `POST /cluster/acme/plugins` — Add ACME plugin configuration.
- `POST /cluster/metrics/server/{id}` — Create a new external metric server config
- `POST /cluster/sdn/controllers` — Create a new sdn controller object.
- `POST /cluster/sdn/vnets` — Create a new sdn vnet object.
- `POST /cluster/sdn/zones` — Create a new sdn zone object.
- `PUT /cluster/acme/plugins/{id}` — Update ACME plugin configuration.
- `PUT /cluster/metrics/server/{id}` — Update metric server configuration.
- `PUT /cluster/sdn/controllers/{controller}` — Update sdn controller object configuration.
- `PUT /cluster/sdn/vnets/{vnet}` — Update sdn vnet object configuration.
- `PUT /cluster/sdn/zones/{zone}` — Update sdn zone object configuration.
- `PUT /nodes/{node}/ceph/pools/{name}`
- `PUT /nodes/{node}/storage/{storage}/content/{volume}` — Update volume attributes

### Changed endpoints (53)

- `DELETE /nodes/{node}/lxc/{vmid}`: added `force`
- `GET /access/roles/{roleid}`: returns object: `+VM.Config.Cloudinit`
- `GET /cluster/ceph/metadata`: added `scope`
- `GET /cluster/firewall/groups/{group}/{pos}`: returns object: `+icmp-type`
- `GET /cluster/firewall/rules/{pos}`: returns object: `+icmp-type`
- `GET /nodes/{node}/firewall/rules/{pos}`: returns object: `+icmp-type`
- `GET /nodes/{node}/lxc/{vmid}/config`: returns object: `+timezone`; returns object: `+debug`
- `GET /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: returns object: `+icmp-type`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+rng0`; returns object: `+keephugepages`
- `GET /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: returns object: `+icmp-type`
- `GET /nodes/{node}/storage/{storage}/content/{volume}`: returns object: `+format`, `+notes`, `+path`, `+size`, `+used`
- `POST /access/domains`: added `filter`, `group_classes`, `group_dn`, `group_filter`, `group_name_attr`, `mode`, `password`, `sync-defaults-options`, `sync_attributes`, `user_classes`; added `case-sensitive`
- `POST /access/users/{userid}/token/{tokenid}`: returns object: `+full-tokenid`
- `POST /cluster/backup`: added `zstd`; added `prune-backups`
- `POST /cluster/config`: changed `link[n]` (format: ~priority)
- `POST /cluster/config/join`: changed `link[n]` (format: ~priority)
- `POST /cluster/config/nodes/{node}`: added `apiversion`, `new_node_ip`; changed `link[n]` (format: ~priority); returns object: `+warnings`
- `POST /cluster/firewall/groups/{group}`: added `icmp-type`
- `POST /cluster/firewall/rules`: added `icmp-type`
- `POST /cluster/sdn/vnets`: added `vlanaware`; changed `tag` (optional: false → true)
- `POST /cluster/sdn/zones`: added `vlan-protocol`
- `POST /nodes/{node}/ceph/osd`: added `crush-device-class`
- `POST /nodes/{node}/ceph/pools`: added `pg_autoscale_mode`
- `POST /nodes/{node}/firewall/rules`: added `icmp-type`
- `POST /nodes/{node}/lxc`: changed `features` (format: +force_rw_sys +mknod), `unused[n]` (format: "pve-volume-id" → {"volume":{"default_key":1,"descripti…); added `timezone`; added `debug`
- `POST /nodes/{node}/lxc/{vmid}/firewall/rules`: added `icmp-type`
- `POST /nodes/{node}/lxc/{vmid}/status/start`: added `debug`
- `POST /nodes/{node}/qemu`: added `rng0`; changed `cpu` (format: {"cputype":{"default":"kvm64","defaul… → "pve-vm-cpu-conf"), `net[n]` (format: ~bridge), `unused[n]` (format: "pve-volume-id" → {"file":{"default_key":1,"description…); changed `net[n]` (format: +mtu); added `keephugepages`; changed `boot` (format: null → "pve-qm-boot"), `usb[n]` (format: ~host)
- `POST /nodes/{node}/qemu/{vmid}/agent/exec`: added `input-data`; changed `command` (optional: false → true)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `rng0`; changed `cpu` (format: {"cputype":{"default":"kvm64","defaul… → "pve-vm-cpu-conf"), `net[n]` (format: ~bridge), `unused[n]` (format: "pve-volume-id" → {"file":{"default_key":1,"description…); changed `net[n]` (format: +mtu); added `keephugepages`; changed `boot` (format: null → "pve-qm-boot"), `usb[n]` (format: ~host)
- `POST /nodes/{node}/qemu/{vmid}/firewall/rules`: added `icmp-type`
- `POST /nodes/{node}/qemu/{vmid}/migrate`: changed `targetstorage` (format: "pve-storage-id" → "storagepair-list")
- `POST /nodes/{node}/qemu/{vmid}/status/start`: added `force-cpu`; changed `targetstorage` (format: null → "storagepair-list")
- `POST /nodes/{node}/qemu/{vmid}/vncproxy`: added `generate-password`; returns null: `+password`
- `POST /nodes/{node}/spiceshell`: added `cmd-opts`
- `POST /nodes/{node}/termproxy`: added `cmd-opts`
- `POST /nodes/{node}/vncshell`: added `cmd-opts`
- `POST /nodes/{node}/vzdump`: added `zstd`; added `prune-backups`
- `POST /storage`: added `datastore`, `fingerprint`; added `encryption-key`; added `prune-backups`; added `port`; returns null → object
- `PUT /access/domains/{realm}`: added `filter`, `group_classes`, `group_dn`, `group_filter`, `group_name_attr`, `mode`, `password`, `sync-defaults-options`, `sync_attributes`, `user_classes`; added `case-sensitive`
- `PUT /cluster/backup/{id}`: added `zstd`; added `prune-backups`
- `PUT /cluster/firewall/groups/{group}/{pos}`: added `icmp-type`
- `PUT /cluster/firewall/options`: changed `log_ratelimit` (format: ~burst)
- `PUT /cluster/firewall/rules/{pos}`: added `icmp-type`
- `PUT /cluster/sdn/vnets/{vnet}`: added `vlanaware`
- `PUT /cluster/sdn/zones/{zone}`: added `vlan-protocol`
- `PUT /nodes/{node}/config`: added `acmedomain[n]`; changed `acme` (format: ~domains); changed `acmedomain[n]` (format: ~alias)
- `PUT /nodes/{node}/firewall/rules/{pos}`: added `icmp-type`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `features` (format: +force_rw_sys +mknod), `unused[n]` (format: "pve-volume-id" → {"volume":{"default_key":1,"descripti…); added `timezone`; added `debug`
- `PUT /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: added `icmp-type`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `rng0`; changed `cpu` (format: {"cputype":{"default":"kvm64","defaul… → "pve-vm-cpu-conf"), `net[n]` (format: ~bridge), `unused[n]` (format: "pve-volume-id" → {"file":{"default_key":1,"description…); changed `net[n]` (format: +mtu); added `keephugepages`; changed `boot` (format: null → "pve-qm-boot"), `usb[n]` (format: ~host)
- `PUT /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: added `icmp-type`
- `PUT /storage/{storage}`: added `fingerprint`; added `encryption-key`; added `prune-backups`; added `port`; returns null → object

## PVE 6.1 (2020-02-13)

### New endpoints (7)

- `DELETE /access/users/{userid}/token/{tokenid}` — Remove API token for a specific user.
- `GET /access/permissions` — Retrieve effective permissions of given user/token.
- `GET /access/users/{userid}/token` — Get user API tokens.
- `GET /access/users/{userid}/token/{tokenid}` — Get specific API token information.
- `POST /access/users/{userid}/token/{tokenid}` — Generate a new API token for a specific user. NOTE: returns API token value, which needs to be stored as it cannot be retrieved afterwards!
- `POST /nodes/{node}/lxc/{vmid}/status/reboot` — Reboot the container by shutting it down, and starting it again. Applies pending changes.
- `PUT /access/users/{userid}/token/{tokenid}` — Update API token for a specific user. NOTE: when 'regenerate' is set, the returned token value needs to be stored as it cannot be retrieved afterwards!

### Changed endpoints (24)

- `GET /access/roles/{roleid}`: returns object: `+SDN.Allocate`, `+SDN.Audit`
- `GET /access/users`: added `full`
- `GET /access/users/{userid}`: returns object: `+tokens`
- `GET /nodes/{node}/config`: added `property`
- `GET /nodes/{node}/firewall/options`: returns object: `+nf_conntrack_tcp_timeout_syn_recv`, `+protection_synflood`, `+protection_synflood_burst`, `+protection_synflood_rate`
- `GET /nodes/{node}/lxc/{vmid}/config`: returns object: `+tags`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+tags`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+tags`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+tags`
- `POST /nodes/{node}/lxc`: added `tags`
- `POST /nodes/{node}/migrateall`: added `with-local-disks`
- `POST /nodes/{node}/network`: added `bond-primary`, `mtu`, `vlan-id`, `vlan-raw-device`
- `POST /nodes/{node}/qemu`: added `tags`; changed `agent` (format: +type)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `tags`; changed `agent` (format: +type)
- `POST /nodes/{node}/qemu/{vmid}/status/start`: added `timeout`
- `POST /storage`: added `mountpoint`
- `PUT /access/acl`: added `tokens`
- `PUT /cluster/options`: changed `ha` (format: ~shutdown_policy); changed `ha` (format: ~shutdown_policy)
- `PUT /nodes/{node}/config`: added `startall-onboot-delay`
- `PUT /nodes/{node}/firewall/options`: added `nf_conntrack_tcp_timeout_syn_recv`, `protection_synflood`, `protection_synflood_burst`, `protection_synflood_rate`
- `PUT /nodes/{node}/lxc/{vmid}/config`: added `tags`
- `PUT /nodes/{node}/network/{iface}`: added `bond-primary`, `mtu`, `vlan-id`, `vlan-raw-device`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `tags`; changed `agent` (format: +type)
- `PUT /storage/{storage}`: added `mountpoint`

## PVE 6.0 (2019-11-18)

### New endpoints (27)

- `DELETE /cluster/sdn`
- `DELETE /cluster/sdn/{sdn}`
- `GET /cluster/ceph` — Cluster ceph index.
- `GET /cluster/ceph/flags` — get the status of all ceph flags
- `GET /cluster/ceph/flags/{flag}` — Get the status of a specific ceph flag.
- `GET /cluster/ceph/metadata` — Get ceph metadata.
- `GET /cluster/ceph/status` — Get ceph status.
- `GET /cluster/config/qdevice` — Get QDevice status
- `GET /cluster/sdn` — Directory index.
- `GET /cluster/sdn/{sdn}`
- `GET /nodes/{node}/ceph/configdb`
- `GET /nodes/{node}/ceph/mgr` — MGR directory index.
- `GET /nodes/{node}/lxc/{vmid}/pending` — Get container configuration, including pending changes.
- `GET /nodes/{node}/qemu/{vmid}/cloudinit/dump` — Get automatically generated cloudinit config.
- `GET /nodes/{node}/qemu/{vmid}/migrate` — Get preconditions for migration.
- `GET /nodes/{node}/sdn` — SDN index.
- `GET /nodes/{node}/sdn/{sdn}`
- `GET /nodes/{node}/sdn/{sdn}/content`
- `POST /cluster/sdn`
- `POST /nodes/{node}/ceph/mgr/{id}` — Create Ceph Manager
- `POST /nodes/{node}/ceph/mon/{monid}` — Create a Ceph Monitor. Also auto-creates a Manager for the first monitor.
- `POST /nodes/{node}/ceph/osd/{osdid}/scrub` — Instruct the OSD to scrub.
- `POST /nodes/{node}/qemu/{vmid}/status/reboot` — Reboot the VM by shutting it down, and starting it again. Applies pending changes.
- `PUT /cluster/ceph/flags` — Set/Unset multiple Ceph flags at once. Each flag is a top-level optional boolean: passing true sets the flag, false unsets it, omitting it leaves the current state untouched. Runs as a worker task; returns a UPID to follow.
- `PUT /cluster/ceph/flags/{flag}` — Set or clear (unset) a specific Ceph flag. Runs synchronously (unlike the bulk PUT /cluster/ceph/flags endpoint, which forks a worker task).
- `PUT /cluster/sdn` — Apply sdn controller changes && reload.
- `PUT /cluster/sdn/{sdn}`

### Changed endpoints (23)

- `DELETE /nodes/{node}/ceph/mon/{monid}`: removed `exclude-manager`
- `DELETE /nodes/{node}/lxc/{vmid}`: added `purge`
- `DELETE /nodes/{node}/qemu/{vmid}`: added `purge`
- `DELETE /nodes/{node}/storage/{storage}/content/{volume}`: added `delay`; returns null → string
- `GET /nodes/{node}/lxc/{vmid}/config`: added `current`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+audio0`, `+spice_enhancements`
- `GET /nodes/{node}/qemu/{vmid}/migrate`: returns object: `+not_allowed_nodes`
- `POST /access/domains`: added `sslversion`
- `POST /cluster/backup`: added `pool`
- `POST /cluster/config`: added `link[n]`; removed `bindnet0_addr`, `bindnet1_addr`, `ring0_addr`, `ring1_addr`
- `POST /cluster/config/join`: added `link[n]`; removed `ring0_addr`, `ring1_addr`
- `POST /cluster/config/nodes/{node}`: added `link[n]`; removed `ring0_addr`, `ring1_addr`
- `POST /nodes/{node}/ceph/osd`: added `db_dev`, `db_size`, `encrypted`, `wal_size`; removed `bluestore`, `fstype`, `journal_dev`
- `POST /nodes/{node}/certificates/custom`: returns object: `+public-key-bits`, `+public-key-type`
- `POST /nodes/{node}/lxc`: changed `mp[n]` (format: +mountoptions), `rootfs` (format: +mountoptions)
- `POST /nodes/{node}/qemu`: changed `cpu` (format: ~flags); added `audio0`, `spice_enhancements`; changed `cpu` (format: ~cputype ~flags), `usb[n]` (format: ~usb3)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~flags); added `audio0`, `spice_enhancements`; changed `cpu` (format: ~cputype ~flags), `usb[n]` (format: ~usb3)
- `POST /nodes/{node}/vzdump`: added `pool`
- `PUT /access/domains/{realm}`: added `sslversion`
- `PUT /access/tfa`: changed `key` (format: null → "pve-tfa-secret")
- `PUT /cluster/backup/{id}`: added `pool`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `mp[n]` (format: +mountoptions), `rootfs` (format: +mountoptions); added `revert`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~flags); added `audio0`, `spice_enhancements`; changed `cpu` (format: ~cputype ~flags), `usb[n]` (format: ~usb3)

## PVE 6.0-0+1 (2019-05-16)

### New endpoints (2)

- `GET /access/users/{userid}/tfa` — Get user TFA types (Personal and Realm).
- `GET /nodes/{node}/journal` — Read Journal

### Changed endpoints (2)

- `POST /nodes/{node}/network`: added `cidr`, `cidr6`
- `PUT /nodes/{node}/network/{iface}`: added `cidr`, `cidr6`

## PVE 5.4 (2019-04-04)

### New endpoints (2)

- `POST /access/tfa`
- `PUT /access/tfa`

### Changed endpoints (40)

- `GET /cluster/firewall/groups/{group}/{pos}`: returns object: `+log`
- `GET /cluster/firewall/options`: returns object: `+log_ratelimit`
- `GET /cluster/firewall/rules/{pos}`: returns object: `+log`
- `GET /nodes/{node}/firewall/options`: returns object: `+log_nf_conntrack`
- `GET /nodes/{node}/firewall/rules/{pos}`: returns object: `+log`
- `GET /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: returns object: `+log`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+lock`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+cicustom`, `+ivshmem`
- `GET /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: returns object: `+log`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+lock`
- `POST /cluster/firewall/groups/{group}`: added `log`
- `POST /cluster/firewall/rules`: added `log`
- `POST /nodes/{node}/firewall/rules`: added `log`
- `POST /nodes/{node}/lxc`: added `unique`; changed `features` (format: ~mount), `net[n]` (format: ~hwaddr)
- `POST /nodes/{node}/lxc/{vmid}/clone`: added `bwlimit`
- `POST /nodes/{node}/lxc/{vmid}/firewall/rules`: added `log`
- `POST /nodes/{node}/lxc/{vmid}/migrate`: added `bwlimit`
- `POST /nodes/{node}/lxc/{vmid}/move_volume`: added `bwlimit`
- `POST /nodes/{node}/qemu`: added `cicustom`, `ivshmem`; changed `ide[n]` (format: +wwn), `net[n]` (format: ~macaddr), `sata[n]` (format: +wwn), `scsi[n]` (format: +wwn)
- `POST /nodes/{node}/qemu/{vmid}/clone`: added `bwlimit`
- `POST /nodes/{node}/qemu/{vmid}/config`: added `cicustom`, `ivshmem`; changed `ide[n]` (format: +wwn), `net[n]` (format: ~macaddr), `sata[n]` (format: +wwn), `scsi[n]` (format: +wwn)
- `POST /nodes/{node}/qemu/{vmid}/firewall/rules`: added `log`
- `POST /nodes/{node}/qemu/{vmid}/migrate`: added `bwlimit`
- `POST /nodes/{node}/qemu/{vmid}/move_disk`: added `bwlimit`
- `POST /nodes/{node}/qemu/{vmid}/status/suspend`: added `statestorage`, `todisk`
- `POST /nodes/{node}/spiceshell`: added `cmd`
- `POST /nodes/{node}/termproxy`: added `cmd`
- `POST /nodes/{node}/vncshell`: added `cmd`
- `POST /storage`: changed `bwlimit` (format: ~clone ~default ~migration ~move ~restore)
- `PUT /cluster/firewall/groups/{group}/{pos}`: added `log`
- `PUT /cluster/firewall/options`: added `log_ratelimit`
- `PUT /cluster/firewall/rules/{pos}`: added `log`
- `PUT /cluster/options`: added `u2f`; changed `bwlimit` (format: ~clone ~default ~migration ~move ~restore), `mac_prefix` (format: null → "mac-prefix")
- `PUT /nodes/{node}/firewall/options`: added `log_nf_conntrack`
- `PUT /nodes/{node}/firewall/rules/{pos}`: added `log`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `features` (format: ~mount), `net[n]` (format: ~hwaddr)
- `PUT /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: added `log`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `cicustom`, `ivshmem`; changed `ide[n]` (format: +wwn), `net[n]` (format: ~macaddr), `sata[n]` (format: +wwn), `scsi[n]` (format: +wwn)
- `PUT /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: added `log`
- `PUT /storage/{storage}`: changed `bwlimit` (format: ~clone ~default ~migration ~move ~restore)

## PVE 5.3 (2019-02-01)

### New endpoints (11)

- `DELETE /nodes/{node}/ceph/mds/{name}` — Destroy Ceph Metadata Server
- `GET /nodes/{node}/ceph/fs` — Directory index.
- `GET /nodes/{node}/ceph/mds` — MDS directory index.
- `GET /nodes/{node}/hardware` — Index of hardware types
- `GET /nodes/{node}/hardware/pci` — List local PCI devices.
- `GET /nodes/{node}/hardware/pci/{pciid}`
- `GET /nodes/{node}/hardware/pci/{pciid}/mdev`
- `POST /nodes/{node}/ceph/fs/{name}` — Create a Ceph filesystem
- `POST /nodes/{node}/ceph/mds/{name}` — Create Ceph Metadata Server (MDS)
- `POST /nodes/{node}/ceph/restart` — Restart ceph services.
- `POST /nodes/{node}/wakeonlan` — Try to wake a node via 'wake on LAN' network packet.

### Changed endpoints (24)

- `GET /access/roles/{roleid}`: returns none → object
- `GET /access/users/{userid}`: returns none → object
- `GET /cluster/firewall/groups/{group}/{pos}`: returns object: `+action`, `+comment`, `+dest`, `+dport`, `+enable`, `+iface`, `+ipversion`, `+macro`, `+proto`, `+source`, `+sport`, `+type`
- `GET /cluster/firewall/rules/{pos}`: returns object: `+action`, `+comment`, `+dest`, `+dport`, `+enable`, `+iface`, `+ipversion`, `+macro`, `+proto`, `+source`, `+sport`, `+type`
- `GET /cluster/ha/resources/{sid}`: returns none → object
- `GET /nodes/{node}/disks/zfs/{name}`: returns object: `+errors`
- `GET /nodes/{node}/firewall/options`: returns object: `+nf_conntrack_allow_invalid`
- `GET /nodes/{node}/firewall/rules/{pos}`: returns object: `+action`, `+comment`, `+dest`, `+dport`, `+enable`, `+iface`, `+ipversion`, `+macro`, `+proto`, `+source`, `+sport`, `+type`
- `GET /nodes/{node}/lxc/{vmid}/config`: added `snapshot`; returns object: `+hookscript`
- `GET /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}`: returns object: `+action`, `+comment`, `+dest`, `+dport`, `+enable`, `+iface`, `+ipversion`, `+macro`, `+proto`, `+source`, `+sport`, `+type`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+arch`; added `snapshot`; returns object: `+hookscript`
- `GET /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}`: returns object: `+action`, `+comment`, `+dest`, `+dport`, `+enable`, `+iface`, `+ipversion`, `+macro`, `+proto`, `+source`, `+sport`, `+type`
- `GET /nodes/{node}/tasks`: added `source`, `typefilter`
- `POST /access/ticket`: returns object: `+clustername`
- `POST /nodes/{node}/ceph/init`: added `cluster-network`
- `POST /nodes/{node}/lxc`: changed `features` (format: +fuse); added `hookscript`
- `POST /nodes/{node}/qemu`: added `arch`; changed `vga` (format: null → {"memory":{"description":"Sets the VG…); added `hookscript`; changed `cpu` (format: +hv-vendor-id), `vga` (format: ~type)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `arch`; changed `vga` (format: null → {"memory":{"description":"Sets the VG…); added `hookscript`; changed `cpu` (format: +hv-vendor-id), `vga` (format: ~type)
- `PUT /access/roles/{roleid}`: changed `privs` (optional: false → true)
- `PUT /cluster/options`: added `ha`
- `PUT /nodes/{node}/config`: added `wakeonlan`
- `PUT /nodes/{node}/firewall/options`: added `nf_conntrack_allow_invalid`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `features` (format: +fuse); added `hookscript`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `arch`; changed `vga` (format: null → {"memory":{"description":"Sets the VG…); added `hookscript`; changed `cpu` (format: +hv-vendor-id), `vga` (format: ~type)

## PVE 5.2 (2018-11-10)

### New endpoints (35)

- `DELETE /cluster/acme/account/{name}` — Deactivate existing ACME account at CA.
- `DELETE /nodes/{node}/certificates/acme/certificate` — Revoke existing certificate from CA.
- `DELETE /nodes/{node}/certificates/custom` — DELETE custom certificate chain and key.
- `GET /cluster/acme` — ACMEAccount index.
- `GET /cluster/acme/account` — ACMEAccount index.
- `GET /cluster/acme/account/{name}` — Return existing ACME account information.
- `GET /cluster/acme/directories` — Get named known ACME directory endpoints.
- `GET /cluster/acme/tos` — Retrieve ACME TermsOfService URL from CA. Deprecated, please use /cluster/acme/meta.
- `GET /nodes/{node}/certificates` — Node index.
- `GET /nodes/{node}/certificates/acme` — ACME index.
- `GET /nodes/{node}/certificates/info` — Get information about node's certificates.
- `GET /nodes/{node}/config` — Get node configuration options.
- `GET /nodes/{node}/disks/directory` — PVE Managed Directory storages.
- `GET /nodes/{node}/disks/lvm` — List LVM Volume Groups
- `GET /nodes/{node}/disks/lvmthin` — List LVM thinpools
- `GET /nodes/{node}/disks/zfs` — List Zpools.
- `GET /nodes/{node}/disks/zfs/{name}` — Get details about a zpool.
- `GET /nodes/{node}/hosts` — Get the content of /etc/hosts.
- `GET /nodes/{node}/qemu/{vmid}/agent/exec-status` — Gets the status of the given pid started by the guest-agent
- `GET /nodes/{node}/qemu/{vmid}/agent/file-read` — Reads the given file via guest agent. Is limited to 16777216 bytes.
- `POST /cluster/acme/account` — Register a new ACME account with CA.
- `POST /nodes/{node}/certificates/acme/certificate` — Order a new certificate from ACME-compatible CA.
- `POST /nodes/{node}/certificates/custom` — Upload or update custom certificate chain and key.
- `POST /nodes/{node}/disks/directory` — Create a Filesystem on an unused disk. Will be mounted under '/mnt/pve/NAME'.
- `POST /nodes/{node}/disks/lvm` — Create an LVM Volume Group
- `POST /nodes/{node}/disks/lvmthin` — Create an LVM thinpool
- `POST /nodes/{node}/disks/zfs` — Create a ZFS pool.
- `POST /nodes/{node}/hosts` — Write /etc/hosts.
- `POST /nodes/{node}/qemu/{vmid}/agent/exec` — Executes the given command in the vm via the guest-agent and returns an object with the pid.
- `POST /nodes/{node}/qemu/{vmid}/agent/file-write` — Writes the given file via guest agent.
- `POST /nodes/{node}/qemu/{vmid}/agent/set-user-password` — Sets the password for the given user to the given password
- `PUT /cluster/acme/account/{name}` — Update existing ACME account information with CA. Note: not specifying any new account information triggers a refresh.
- `PUT /nodes/{node}/certificates/acme/certificate` — Renew existing certificate from CA.
- `PUT /nodes/{node}/config` — Set node configuration options.
- `PUT /nodes/{node}/network` — Reload network configuration

### Changed endpoints (25)

- `GET /cluster/config/totem`: returns object: reordered
- `GET /cluster/firewall/options`: returns object: `+ebtables`
- `GET /cluster/options`: returns object: reordered
- `GET /nodes/{node}/config`: returns object: reordered
- `GET /nodes/{node}/disks/list`: added `skipsmart`, `type`
- `GET /nodes/{node}/lxc/{vmid}/config`: returns object: `+arch`, `+cmode`, `+console`, `+cores`, `+cpulimit`, `+cpuunits`, `+description`, `+features`, `+hostname`, `+lock`, `+lxc`, `+memory`, `+mp[n]`, `+nameserver`, `+net[n]`, `+onboot`, `+ostype`, `+protection`, `+rootfs`, `+searchdomain`, `+startup`, `+swap`, `+template`, `+tty`, `+unprivileged`, `+unused[n]`
- `GET /nodes/{node}/lxc/{vmid}/status/current`: returns object: `+cpus`, `+ha`, `+maxdisk`, `+maxmem`, `+maxswap`, `+name`, `+status`, `+uptime`, `+vmid`
- `GET /nodes/{node}/qemu/{vmid}/config`: returns object: `+acpi`, `+agent`, `+args`, `+autostart`, `+balloon`, `+bios`, `+boot`, `+bootdisk`, `+cdrom`, `+cipassword`, `+citype`, `+ciuser`, `+cores`, `+cpu`, `+cpulimit`, `+cpuunits`, `+description`, `+efidisk0`, `+freeze`, `+hostpci[n]`, `+hotplug`, `+hugepages`, `+ide[n]`, `+ipconfig[n]`, `+keyboard`, `+kvm`, `+localtime`, `+lock`, `+machine`, `+memory`, `+migrate_downtime`, `+migrate_speed`, `+name`, `+nameserver`, `+net[n]`, `+numa`, `+numa[n]`, `+onboot`, `+ostype`, `+parallel[n]`, `+protection`, `+reboot`, `+sata[n]`, `+scsi[n]`, `+scsihw`, `+searchdomain`, `+serial[n]`, `+shares`, `+smbios1`, `+smp`, `+sockets`, `+sshkeys`, `+startdate`, `+startup`, `+tablet`, `+tdf`, `+template`, `+unused[n]`, `+usb[n]`, `+vcpus`, `+vga`, `+virtio[n]`, `+vmgenid`, `+vmstatestorage`, `+watchdog`
- `GET /nodes/{node}/qemu/{vmid}/status/current`: returns object: `+agent`, `+cpus`, `+ha`, `+maxdisk`, `+maxmem`, `+name`, `+pid`, `+qmpstatus`, `+spice`, `+status`, `+uptime`, `+vmid`
- `GET /nodes/{node}/replication/{id}/status`: returns object: reordered
- `GET /nodes/{node}/services/{service}/state`: returns object: reordered
- `GET /nodes/{node}/status`: returns object: reordered
- `GET /nodes/{node}/storage/{storage}/status`: returns object: reordered
- `GET /storage/{storage}`: returns none → object
- `POST /cluster/replication`: added `source`
- `POST /nodes/{node}/execute`: returns array: reordered
- `POST /nodes/{node}/lxc`: added `start`; added `features`
- `POST /nodes/{node}/qemu`: added `start`; added `efidisk0`, `vmgenid`; changed `agent` (type: "boolean" → "string"; format: null → {"enabled":{"default":0,"default_key"…), `cpu` (format: ~flags); changed `ide[n]` (format: +ssd), `sata[n]` (format: +ssd), `scsi[n]` (format: +ssd)
- `POST /nodes/{node}/qemu/{vmid}/config`: added `efidisk0`, `vmgenid`; changed `agent` (type: "boolean" → "string"; format: null → {"enabled":{"default":0,"default_key"…), `cpu` (format: ~flags); changed `ide[n]` (format: +ssd), `sata[n]` (format: +ssd), `scsi[n]` (format: +ssd)
- `POST /storage`: added `fuse`, `subdir`; added `lio_tpg`
- `PUT /cluster/firewall/options`: added `ebtables`
- `PUT /cluster/replication/{id}`: added `source`
- `PUT /nodes/{node}/lxc/{vmid}/config`: added `features`
- `PUT /nodes/{node}/qemu/{vmid}/config`: added `efidisk0`, `vmgenid`; changed `agent` (type: "boolean" → "string"; format: null → {"enabled":{"default":0,"default_key"…), `cpu` (format: ~flags); changed `ide[n]` (format: +ssd), `sata[n]` (format: +ssd), `scsi[n]` (format: +ssd)
- `PUT /storage/{storage}`: added `fuse`, `subdir`; added `lio_tpg`

## PVE 5.1 (2018-03-22)

### New endpoints (31)

- `DELETE /cluster/config/nodes/{node}` — Removes a node from the cluster configuration.
- `GET /cluster/config/join` — Get information needed to join this cluster over the connected node.
- `GET /nodes/{node}/qemu/{vmid}/agent` — QEMU Guest Agent command index.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-fsinfo` — Execute get-fsinfo.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-host-name` — Execute get-host-name.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-memory-block-info` — Execute get-memory-block-info.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-memory-blocks` — Execute get-memory-blocks.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-osinfo` — Execute get-osinfo.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-time` — Execute get-time.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-timezone` — Execute get-timezone.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-users` — Execute get-users.
- `GET /nodes/{node}/qemu/{vmid}/agent/get-vcpus` — Execute get-vcpus.
- `GET /nodes/{node}/qemu/{vmid}/agent/info` — Execute info.
- `GET /nodes/{node}/qemu/{vmid}/agent/network-get-interfaces` — Execute network-get-interfaces.
- `GET /nodes/{node}/scan/cifs` — Scan remote CIFS server.
- `POST /cluster/config` — Generate new cluster configuration. If no links given, default to local IP address as link0.
- `POST /cluster/config/join` — Joins this node into an existing cluster. If no links are given, default to IP resolved by node's hostname on single link (fallback fails for clusters with multiple links).
- `POST /cluster/config/nodes/{node}` — Adds a node to the cluster configuration. This call is for internal use.
- `POST /nodes/{node}/lxc/{vmid}/move_volume` — Move a rootfs-/mp-volume to a different storage or to a different container.
- `POST /nodes/{node}/lxc/{vmid}/termproxy` — Creates a TCP proxy connection.
- `POST /nodes/{node}/qemu/{vmid}/agent/fsfreeze-freeze` — Execute fsfreeze-freeze.
- `POST /nodes/{node}/qemu/{vmid}/agent/fsfreeze-status` — Execute fsfreeze-status.
- `POST /nodes/{node}/qemu/{vmid}/agent/fsfreeze-thaw` — Execute fsfreeze-thaw.
- `POST /nodes/{node}/qemu/{vmid}/agent/fstrim` — Execute fstrim.
- `POST /nodes/{node}/qemu/{vmid}/agent/ping` — Execute ping.
- `POST /nodes/{node}/qemu/{vmid}/agent/shutdown` — Execute shutdown.
- `POST /nodes/{node}/qemu/{vmid}/agent/suspend-disk` — Execute suspend-disk.
- `POST /nodes/{node}/qemu/{vmid}/agent/suspend-hybrid` — Execute suspend-hybrid.
- `POST /nodes/{node}/qemu/{vmid}/agent/suspend-ram` — Execute suspend-ram.
- `POST /nodes/{node}/qemu/{vmid}/termproxy` — Creates a TCP proxy connections.
- `POST /nodes/{node}/termproxy` — Creates a VNC Shell proxy.

### Changed endpoints (12)

- `GET /nodes/{node}/storage`: added `format`
- `GET /nodes/{node}/syslog`: added `service`
- `POST /nodes/{node}/ceph/mon`: added `mon-address`
- `POST /nodes/{node}/lxc`: added `bwlimit`
- `POST /nodes/{node}/lxc/{vmid}/clone`: added `target`; removed `experimental`
- `POST /nodes/{node}/lxc/{vmid}/template`: removed `experimental`
- `POST /nodes/{node}/qemu`: changed `cpu` (format: ~cputype +flags); added `bwlimit`, `cipassword`, `citype`, `ciuser`, `ipconfig[n]`, `nameserver`, `searchdomain`, `sshkeys`; changed `cpu` (format: ~cputype), `ide[n]` (format: ~snapshot +shared), `sata[n]` (format: ~snapshot +shared), `scsi[n]` (format: ~snapshot +shared), `virtio[n]` (format: ~snapshot +shared)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype +flags); added `cipassword`, `citype`, `ciuser`, `ipconfig[n]`, `nameserver`, `searchdomain`, `sshkeys`; changed `cpu` (format: ~cputype), `ide[n]` (format: ~snapshot +shared), `sata[n]` (format: ~snapshot +shared), `scsi[n]` (format: ~snapshot +shared), `virtio[n]` (format: ~snapshot +shared)
- `POST /storage`: added `bwlimit`, `domain`, `password`, `share`, `smbversion`
- `PUT /cluster/options`: added `bwlimit`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype +flags); added `cipassword`, `citype`, `ciuser`, `ipconfig[n]`, `nameserver`, `searchdomain`, `sshkeys`; changed `cpu` (format: ~cputype), `ide[n]` (format: ~snapshot +shared), `sata[n]` (format: ~snapshot +shared), `scsi[n]` (format: ~snapshot +shared), `virtio[n]` (format: ~snapshot +shared)
- `PUT /storage/{storage}`: added `bwlimit`, `domain`, `password`, `smbversion`

## PVE 5.0 (2017-10-19)

### New endpoints (13)

- `DELETE /cluster/replication/{id}` — Mark replication job for removal.
- `DELETE /nodes/{node}/ceph/mgr/{id}` — Destroy Ceph Manager.
- `GET /cluster/replication` — List replication jobs.
- `GET /cluster/replication/{id}` — Read replication job configuration.
- `GET /nodes/{node}/ceph/rules` — List ceph rules.
- `GET /nodes/{node}/replication` — List status of all replication jobs on this node.
- `GET /nodes/{node}/replication/{id}` — Directory index.
- `GET /nodes/{node}/replication/{id}/log` — Read replication job log.
- `GET /nodes/{node}/replication/{id}/status` — Get replication job status.
- `POST /cluster/replication` — Create a new replication job
- `POST /nodes/{node}/ceph/mgr`
- `POST /nodes/{node}/replication/{id}/schedule_now` — Schedule replication job to start as soon as possible.
- `PUT /cluster/replication/{id}` — Update replication job configuration.

### Changed endpoints (17)

- `DELETE /nodes/{node}/ceph/mon/{monid}`: added `exclude-manager`; changed `monid` (type: "integer" → "string")
- `DELETE /nodes/{node}/ceph/pools/{name}`: added `remove_storages`; returns null → string
- `POST /access/domains`: added `capath`, `cert`, `certkey`, `verify`
- `POST /nodes/{node}/ceph/init`: added `disable_cephx`, `min_size`
- `POST /nodes/{node}/ceph/mon`: added `exclude-manager`, `id`
- `POST /nodes/{node}/ceph/osd`: added `bluestore`; added `wal_dev`
- `POST /nodes/{node}/ceph/pools`: added `add_storages`, `application`, `crush_rule`; removed `crush_ruleset`; returns null → string
- `POST /nodes/{node}/lxc`: changed `mp[n]` (format: +replicate), `rootfs` (format: +replicate); changed `net[n]` (format: ~ip ~ip6)
- `POST /nodes/{node}/lxc/{vmid}/vncproxy`: added `height`, `width`
- `POST /nodes/{node}/qemu`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `sata[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `scsi[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate +rerror), `virtio[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate); added `vmstatestorage`
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `sata[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `scsi[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate +rerror), `virtio[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate); added `vmstatestorage`
- `POST /nodes/{node}/vncshell`: added `height`, `width`
- `POST /storage`: changed `is_mountpoint` (type: "boolean" → "string")
- `PUT /access/domains/{realm}`: added `capath`, `cert`, `certkey`, `verify`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `mp[n]` (format: +replicate), `rootfs` (format: +replicate); changed `net[n]` (format: ~ip ~ip6)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `sata[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate), `scsi[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate +rerror), `virtio[n]` (format: ~bps_rd_length ~bps_wr_length ~iops_rd_length ~iops_wr_length +bps_rd_max_length +bps_wr_max_length +iops_rd_max_length +iops_wr_max_length +replicate); added `vmstatestorage`
- `PUT /storage/{storage}`: added `monhost`; changed `is_mountpoint` (type: "boolean" → "string")

## PVE 4.4 (2017-01-27)

### Changed endpoints (9)

- `POST /nodes/{node}/lxc/{vmid}/migrate`: added `restart`, `timeout`
- `POST /nodes/{node}/migrateall`: added `vms`
- `POST /nodes/{node}/qemu`: changed `scsi[n]` (format: +scsiblock)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `scsi[n]` (format: +scsiblock)
- `POST /nodes/{node}/qemu/{vmid}/migrate`: added `targetstorage`, `with-local-disks`
- `POST /nodes/{node}/qemu/{vmid}/status/start`: added `targetstorage`
- `POST /nodes/{node}/startall`: added `vms`
- `POST /nodes/{node}/stopall`: added `vms`
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `scsi[n]` (format: +scsiblock)

## PVE 4.3 (2016-12-01)

### New endpoints (11)

- `DELETE /nodes/{node}/ceph/flags/{flag}`
- `GET /cluster/config` — Directory index.
- `GET /cluster/config/nodes` — Corosync node list.
- `GET /cluster/config/totem` — Get corosync totem protocol settings.
- `GET /nodes/{node}/ceph/flags`
- `GET /nodes/{node}/disks` — Node index.
- `GET /nodes/{node}/disks/list` — List local disks.
- `GET /nodes/{node}/disks/smart` — Get SMART Health of a disk.
- `POST /nodes/{node}/ceph/flags/{flag}`
- `POST /nodes/{node}/disks/initgpt` — Initialize Disk with GPT
- `POST /nodes/{node}/qemu/{vmid}/agent` — Execute QEMU Guest Agent commands.

### Changed endpoints (11)

- `GET /nodes/{node}/disks/smart`: returns object: `+attributes`, `+health`, `+text`, `+type`
- `POST /nodes/{node}/lxc`: added `cores`; changed `mp[n]` (format: ~backup ~mp ~ro +shared), `rootfs` (format: ~ro +shared)
- `POST /nodes/{node}/qemu`: changed `cdrom` (format: "pve-qm-drive" → "pve-qm-ide"), `ide[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `sata[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `scsi[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `virtio[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length); changed `cpu` (format: ~cputype), `net[n]` (format: ~model)
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `cdrom` (format: "pve-qm-drive" → "pve-qm-ide"), `ide[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `sata[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `scsi[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `virtio[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length); changed `cpu` (format: ~cputype), `net[n]` (format: ~model)
- `POST /nodes/{node}/qemu/{vmid}/migrate`: added `migration_network`, `migration_type`
- `POST /nodes/{node}/qemu/{vmid}/status/start`: added `migration_network`, `migration_type`
- `POST /storage`: added `is_mountpoint`
- `PUT /cluster/options`: added `migration`
- `PUT /nodes/{node}/lxc/{vmid}/config`: added `cores`; changed `mp[n]` (format: ~backup ~mp ~ro +shared), `rootfs` (format: ~ro +shared)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `cdrom` (format: "pve-qm-drive" → "pve-qm-ide"), `ide[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `sata[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `scsi[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length), `virtio[n]` (format: ~bps ~bps_rd ~bps_wr ~format ~iops ~iops_max ~iops_rd ~iops_rd_max ~iops_wr ~iops_wr_max ~mbps ~mbps_max ~mbps_rd ~mbps_rd_max ~mbps_wr ~mbps_wr_max +bps_max_length +bps_rd_length +bps_wr_length +iops_max_length +iops_rd_length +iops_wr_length); changed `cpu` (format: ~cputype), `net[n]` (format: ~model)
- `PUT /storage/{storage}`: added `is_mountpoint`

## PVE 4.2 (2016-09-05)

### New endpoints (309)

- `DELETE /access/domains/{realm}` — Delete an authentication server.
- `DELETE /access/groups/{groupid}` — Delete group.
- `DELETE /access/roles/{roleid}` — Delete role.
- `DELETE /access/users/{userid}` — Delete user.
- `DELETE /cluster/backup/{id}` — Delete vzdump backup job definition.
- `DELETE /cluster/firewall/aliases/{name}` — Remove IP or Network alias.
- `DELETE /cluster/firewall/groups/{group}` — Delete security group.
- `DELETE /cluster/firewall/groups/{group}/{pos}` — Delete rule.
- `DELETE /cluster/firewall/ipset/{name}` — Delete IPSet
- `DELETE /cluster/firewall/ipset/{name}/{cidr}` — Remove IP or Network from IPSet.
- `DELETE /cluster/firewall/rules/{pos}` — Delete rule.
- `DELETE /cluster/ha/groups/{group}` — Delete ha group configuration. (deprecated in favor of HA rules)
- `DELETE /cluster/ha/resources/{sid}` — Delete resource configuration.
- `DELETE /nodes/{node}/ceph/mon/{monid}` — Destroy a Ceph Monitor. Refuses to remove the last monitor of the cluster. Does not destroy any Manager on the same node; use /nodes/{node}/ceph/mgr/{id} for that.
- `DELETE /nodes/{node}/ceph/osd/{osdid}` — Destroy OSD
- `DELETE /nodes/{node}/ceph/pools/{name}`
- `DELETE /nodes/{node}/firewall/rules/{pos}` — Delete rule.
- `DELETE /nodes/{node}/lxc/{vmid}` — Destroy the container (also delete all uses files).
- `DELETE /nodes/{node}/lxc/{vmid}/firewall/aliases/{name}` — Remove IP or Network alias.
- `DELETE /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}` — Delete IPSet
- `DELETE /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}/{cidr}` — Remove IP or Network from IPSet.
- `DELETE /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}` — Delete rule.
- `DELETE /nodes/{node}/lxc/{vmid}/snapshot/{snapname}` — Delete a LXC snapshot.
- `DELETE /nodes/{node}/network` — Revert network configuration changes.
- `DELETE /nodes/{node}/network/{iface}` — Delete network device configuration
- `DELETE /nodes/{node}/qemu/{vmid}` — Destroy the VM and  all used/owned volumes. Removes any VM specific permissions and firewall rules
- `DELETE /nodes/{node}/qemu/{vmid}/firewall/aliases/{name}` — Remove IP or Network alias.
- `DELETE /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}` — Delete IPSet
- `DELETE /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}/{cidr}` — Remove IP or Network from IPSet.
- `DELETE /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}` — Delete rule.
- `DELETE /nodes/{node}/qemu/{vmid}/snapshot/{snapname}` — Delete a VM snapshot.
- `DELETE /nodes/{node}/storage/{storage}/content/{volume}` — Delete volume
- `DELETE /nodes/{node}/tasks/{upid}` — Stop a task.
- `DELETE /pools/{poolid}` — Delete pool (deprecated, no support for nested pools, use 'DELETE /pools/?poolid={poolid}').
- `DELETE /storage/{storage}` — Delete storage configuration.
- `GET /access` — Directory index.
- `GET /access/acl` — Get Access Control List (ACLs).
- `GET /access/domains` — Authentication domain index.
- `GET /access/domains/{realm}` — Get auth server configuration.
- `GET /access/groups` — Group index.
- `GET /access/groups/{groupid}` — Get group configuration.
- `GET /access/roles` — Role index.
- `GET /access/roles/{roleid}` — Get role configuration.
- `GET /access/ticket` — Dummy. Useful for formatters which want to provide a login page.
- `GET /access/users` — User index.
- `GET /access/users/{userid}` — Get user configuration.
- `GET /cluster` — Cluster index.
- `GET /cluster/backup` — List vzdump backup schedule.
- `GET /cluster/backup/{id}` — Read vzdump backup job definition.
- `GET /cluster/firewall` — Directory index.
- `GET /cluster/firewall/aliases` — List aliases
- `GET /cluster/firewall/aliases/{name}` — Read alias.
- `GET /cluster/firewall/groups` — List security groups.
- `GET /cluster/firewall/groups/{group}` — List rules.
- `GET /cluster/firewall/groups/{group}/{pos}` — Get single rule data.
- `GET /cluster/firewall/ipset` — List IPSets
- `GET /cluster/firewall/ipset/{name}` — List IPSet content
- `GET /cluster/firewall/ipset/{name}/{cidr}` — Read IP or Network settings from IPSet.
- `GET /cluster/firewall/macros` — List available macros
- `GET /cluster/firewall/options` — Get Firewall options.
- `GET /cluster/firewall/refs` — Lists possible IPSet/Alias reference which are allowed in source/dest properties.
- `GET /cluster/firewall/rules` — List rules.
- `GET /cluster/firewall/rules/{pos}` — Get single rule data.
- `GET /cluster/ha` — Directory index.
- `GET /cluster/ha/groups` — Get HA groups. (deprecated in favor of HA rules)
- `GET /cluster/ha/groups/{group}` — Read ha group configuration. (deprecated in favor of HA rules)
- `GET /cluster/ha/resources` — List HA resources.
- `GET /cluster/ha/resources/{sid}` — Read resource configuration.
- `GET /cluster/ha/status` — Directory index.
- `GET /cluster/ha/status/current` — Get HA manager status.
- `GET /cluster/ha/status/manager_status` — Get full HA manager status, including LRM status.
- `GET /cluster/log` — Read cluster log
- `GET /cluster/nextid` — Get next free VMID. Pass a VMID to assert that its free (at time of check).
- `GET /cluster/options` — Get datacenter options. Without 'Sys.Audit' on '/' not all options are returned.
- `GET /cluster/resources` — Resources index (cluster wide).
- `GET /cluster/status` — Get cluster status information.
- `GET /cluster/tasks` — List recent tasks (cluster wide).
- `GET /nodes` — Cluster node index.
- `GET /nodes/{node}` — Node index.
- `GET /nodes/{node}/aplinfo` — Get list of appliances.
- `GET /nodes/{node}/apt` — Directory index for apt (Advanced Package Tool).
- `GET /nodes/{node}/apt/changelog` — Get package changelogs.
- `GET /nodes/{node}/apt/update` — List available updates.
- `GET /nodes/{node}/apt/versions` — Get package information for important Proxmox packages.
- `GET /nodes/{node}/ceph` — Directory index.
- `GET /nodes/{node}/ceph/config`
- `GET /nodes/{node}/ceph/crush` — Get OSD crush map
- `GET /nodes/{node}/ceph/disks`
- `GET /nodes/{node}/ceph/log` — Read ceph log
- `GET /nodes/{node}/ceph/mon` — Get Ceph monitor list.
- `GET /nodes/{node}/ceph/osd` — Get Ceph osd list/tree.
- `GET /nodes/{node}/ceph/pools`
- `GET /nodes/{node}/ceph/status` — Get the Ceph cluster status (raw 'ceph status' output). The response is cluster-wide and identical to /cluster/ceph/status; this node-level alias exists for operator convenience.
- `GET /nodes/{node}/dns` — Read DNS settings.
- `GET /nodes/{node}/firewall` — Directory index.
- `GET /nodes/{node}/firewall/log` — Read firewall log
- `GET /nodes/{node}/firewall/options` — Get host firewall options.
- `GET /nodes/{node}/firewall/rules` — List rules.
- `GET /nodes/{node}/firewall/rules/{pos}` — Get single rule data.
- `GET /nodes/{node}/lxc` — LXC container index (per node).
- `GET /nodes/{node}/lxc/{vmid}` — Directory index
- `GET /nodes/{node}/lxc/{vmid}/config` — Get container configuration.
- `GET /nodes/{node}/lxc/{vmid}/feature` — Check if feature for virtual machine is available.
- `GET /nodes/{node}/lxc/{vmid}/firewall` — Directory index.
- `GET /nodes/{node}/lxc/{vmid}/firewall/aliases` — List aliases
- `GET /nodes/{node}/lxc/{vmid}/firewall/aliases/{name}` — Read alias.
- `GET /nodes/{node}/lxc/{vmid}/firewall/ipset` — List IPSets
- `GET /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}` — List IPSet content
- `GET /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}/{cidr}` — Read IP or Network settings from IPSet.
- `GET /nodes/{node}/lxc/{vmid}/firewall/log` — Read firewall log
- `GET /nodes/{node}/lxc/{vmid}/firewall/options` — Get VM firewall options.
- `GET /nodes/{node}/lxc/{vmid}/firewall/refs` — Lists possible IPSet/Alias reference which are allowed in source/dest properties.
- `GET /nodes/{node}/lxc/{vmid}/firewall/rules` — List rules.
- `GET /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}` — Get single rule data.
- `GET /nodes/{node}/lxc/{vmid}/rrd` — Read VM RRD statistics (returns PNG)
- `GET /nodes/{node}/lxc/{vmid}/rrddata` — Read VM RRD statistics
- `GET /nodes/{node}/lxc/{vmid}/snapshot` — List all snapshots.
- `GET /nodes/{node}/lxc/{vmid}/snapshot/{snapname}`
- `GET /nodes/{node}/lxc/{vmid}/snapshot/{snapname}/config` — Get snapshot configuration
- `GET /nodes/{node}/lxc/{vmid}/status` — Directory index
- `GET /nodes/{node}/lxc/{vmid}/status/current` — Get virtual machine status.
- `GET /nodes/{node}/lxc/{vmid}/vncwebsocket` — Opens a websocket for VNC traffic.
- `GET /nodes/{node}/netstat` — Read tap/vm network device interface counters
- `GET /nodes/{node}/network` — List available networks
- `GET /nodes/{node}/network/{iface}` — Read network device configuration
- `GET /nodes/{node}/qemu` — Virtual machine index (per node).
- `GET /nodes/{node}/qemu/{vmid}` — Directory index
- `GET /nodes/{node}/qemu/{vmid}/config` — Get the virtual machine configuration with pending configuration changes applied. Set the 'current' parameter to get the current configuration instead.
- `GET /nodes/{node}/qemu/{vmid}/feature` — Check if feature for virtual machine is available.
- `GET /nodes/{node}/qemu/{vmid}/firewall` — Directory index.
- `GET /nodes/{node}/qemu/{vmid}/firewall/aliases` — List aliases
- `GET /nodes/{node}/qemu/{vmid}/firewall/aliases/{name}` — Read alias.
- `GET /nodes/{node}/qemu/{vmid}/firewall/ipset` — List IPSets
- `GET /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}` — List IPSet content
- `GET /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}/{cidr}` — Read IP or Network settings from IPSet.
- `GET /nodes/{node}/qemu/{vmid}/firewall/log` — Read firewall log
- `GET /nodes/{node}/qemu/{vmid}/firewall/options` — Get VM firewall options.
- `GET /nodes/{node}/qemu/{vmid}/firewall/refs` — Lists possible IPSet/Alias reference which are allowed in source/dest properties.
- `GET /nodes/{node}/qemu/{vmid}/firewall/rules` — List rules.
- `GET /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}` — Get single rule data.
- `GET /nodes/{node}/qemu/{vmid}/pending` — Get the virtual machine configuration with both current and pending values.
- `GET /nodes/{node}/qemu/{vmid}/rrd` — Read VM RRD statistics (returns PNG)
- `GET /nodes/{node}/qemu/{vmid}/rrddata` — Read VM RRD statistics
- `GET /nodes/{node}/qemu/{vmid}/snapshot` — List all snapshots.
- `GET /nodes/{node}/qemu/{vmid}/snapshot/{snapname}`
- `GET /nodes/{node}/qemu/{vmid}/snapshot/{snapname}/config` — Get snapshot configuration
- `GET /nodes/{node}/qemu/{vmid}/status` — Directory index
- `GET /nodes/{node}/qemu/{vmid}/status/current` — Get virtual machine status.
- `GET /nodes/{node}/qemu/{vmid}/vncwebsocket` — Opens a websocket for VNC traffic.
- `GET /nodes/{node}/report` — Gather various systems information about a node
- `GET /nodes/{node}/rrd` — Read node RRD statistics (returns PNG)
- `GET /nodes/{node}/rrddata` — Read node RRD statistics
- `GET /nodes/{node}/scan` — Index of available scan methods
- `GET /nodes/{node}/scan/glusterfs`
- `GET /nodes/{node}/scan/iscsi` — Scan remote iSCSI server.
- `GET /nodes/{node}/scan/lvm` — List local LVM volume groups.
- `GET /nodes/{node}/scan/lvmthin` — List local LVM Thin Pools.
- `GET /nodes/{node}/scan/nfs` — Scan remote NFS server.
- `GET /nodes/{node}/scan/usb`
- `GET /nodes/{node}/scan/zfs` — Scan zfs pool list on local node.
- `GET /nodes/{node}/services` — Service list.
- `GET /nodes/{node}/services/{service}` — Directory index
- `GET /nodes/{node}/services/{service}/state` — Read service properties
- `GET /nodes/{node}/status` — Read node status
- `GET /nodes/{node}/storage` — Get status for all datastores.
- `GET /nodes/{node}/storage/{storage}`
- `GET /nodes/{node}/storage/{storage}/content` — List storage content.
- `GET /nodes/{node}/storage/{storage}/content/{volume}` — Get volume attributes
- `GET /nodes/{node}/storage/{storage}/rrd` — Read storage RRD statistics (returns PNG).
- `GET /nodes/{node}/storage/{storage}/rrddata` — Read storage RRD statistics.
- `GET /nodes/{node}/storage/{storage}/status` — Read storage status.
- `GET /nodes/{node}/subscription` — Read subscription info.
- `GET /nodes/{node}/syslog` — Read system log
- `GET /nodes/{node}/tasks` — Read task list for one node (finished tasks).
- `GET /nodes/{node}/tasks/{upid}`
- `GET /nodes/{node}/tasks/{upid}/log` — Read task log.
- `GET /nodes/{node}/tasks/{upid}/status` — Read task status.
- `GET /nodes/{node}/time` — Read server time and time zone settings.
- `GET /nodes/{node}/version` — API version details
- `GET /nodes/{node}/vncwebsocket` — Opens a websocket for VNC traffic.
- `GET /nodes/{node}/vzdump/extractconfig` — Extract configuration from vzdump backup archive.
- `GET /pools` — List pools or get pool configuration.
- `GET /pools/{poolid}` — Get pool configuration (deprecated, no support for nested pools, use 'GET /pools/?poolid={poolid}').
- `GET /storage` — Storage index.
- `GET /storage/{storage}` — Read storage configuration.
- `GET /version` — API version details, including some parts of the global datacenter config.
- `POST /access/domains` — Add an authentication server.
- `POST /access/groups` — Create new group.
- `POST /access/roles` — Create new role.
- `POST /access/ticket` — Create or verify authentication ticket.
- `POST /access/users` — Create new user.
- `POST /cluster/backup` — Create new vzdump backup job.
- `POST /cluster/firewall/aliases` — Create IP or Network Alias.
- `POST /cluster/firewall/groups` — Create new security group.
- `POST /cluster/firewall/groups/{group}` — Create new rule.
- `POST /cluster/firewall/ipset` — Create new IPSet
- `POST /cluster/firewall/ipset/{name}` — Add IP or Network to IPSet.
- `POST /cluster/firewall/rules` — Create new rule.
- `POST /cluster/ha/groups` — Create a new HA group. (deprecated in favor of HA rules)
- `POST /cluster/ha/resources` — Create a new HA resource.
- `POST /cluster/ha/resources/{sid}/migrate` — Request resource migration (online) to another node.
- `POST /cluster/ha/resources/{sid}/relocate` — Request resource relocation to another node. This stops the service on the old node, and restarts it on the target node.
- `POST /nodes/{node}/aplinfo` — Download appliance templates.
- `POST /nodes/{node}/apt/update` — This is used to resynchronize the package index files from their sources (apt-get update).
- `POST /nodes/{node}/ceph/init` — Create the initial Ceph default configuration and set up symlinks. Idempotent on re-call: if a [global] section already exists in ceph.conf, the existing fsid / auth / pool defaults are preserved and most parameters are silently ignored.
- `POST /nodes/{node}/ceph/mon`
- `POST /nodes/{node}/ceph/osd` — Create OSD
- `POST /nodes/{node}/ceph/osd/{osdid}/in` — ceph osd in
- `POST /nodes/{node}/ceph/osd/{osdid}/out` — ceph osd out
- `POST /nodes/{node}/ceph/pools`
- `POST /nodes/{node}/ceph/start` — Start ceph services.
- `POST /nodes/{node}/ceph/stop` — Stop ceph services.
- `POST /nodes/{node}/execute` — Execute multiple commands in order, root only.
- `POST /nodes/{node}/firewall/rules` — Create new rule.
- `POST /nodes/{node}/lxc` — Create or restore a container.
- `POST /nodes/{node}/lxc/{vmid}/clone` — Create a container clone/copy
- `POST /nodes/{node}/lxc/{vmid}/firewall/aliases` — Create IP or Network Alias.
- `POST /nodes/{node}/lxc/{vmid}/firewall/ipset` — Create new IPSet
- `POST /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}` — Add IP or Network to IPSet.
- `POST /nodes/{node}/lxc/{vmid}/firewall/rules` — Create new rule.
- `POST /nodes/{node}/lxc/{vmid}/migrate` — Migrate the container to another node. Creates a new migration task.
- `POST /nodes/{node}/lxc/{vmid}/snapshot` — Snapshot a container.
- `POST /nodes/{node}/lxc/{vmid}/snapshot/{snapname}/rollback` — Rollback LXC state to specified snapshot.
- `POST /nodes/{node}/lxc/{vmid}/spiceproxy` — Returns a SPICE configuration to connect to the CT.
- `POST /nodes/{node}/lxc/{vmid}/status/resume` — Resume the container.
- `POST /nodes/{node}/lxc/{vmid}/status/shutdown` — Shutdown the container. This will trigger a clean shutdown of the container, see lxc-stop(1) for details.
- `POST /nodes/{node}/lxc/{vmid}/status/start` — Start the container.
- `POST /nodes/{node}/lxc/{vmid}/status/stop` — Stop the container. This will abruptly stop all processes running in the container.
- `POST /nodes/{node}/lxc/{vmid}/status/suspend` — Suspend the container. This is experimental.
- `POST /nodes/{node}/lxc/{vmid}/template` — Create a Template.
- `POST /nodes/{node}/lxc/{vmid}/vncproxy` — Creates a TCP VNC proxy connections.
- `POST /nodes/{node}/migrateall` — Migrate all VMs and Containers.
- `POST /nodes/{node}/network` — Create network device configuration
- `POST /nodes/{node}/qemu` — Create or restore a virtual machine.
- `POST /nodes/{node}/qemu/{vmid}/clone` — Create a copy of virtual machine/template.
- `POST /nodes/{node}/qemu/{vmid}/config` — Set virtual machine options (asynchronous API).
- `POST /nodes/{node}/qemu/{vmid}/firewall/aliases` — Create IP or Network Alias.
- `POST /nodes/{node}/qemu/{vmid}/firewall/ipset` — Create new IPSet
- `POST /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}` — Add IP or Network to IPSet.
- `POST /nodes/{node}/qemu/{vmid}/firewall/rules` — Create new rule.
- `POST /nodes/{node}/qemu/{vmid}/migrate` — Migrate virtual machine. Creates a new migration task.
- `POST /nodes/{node}/qemu/{vmid}/monitor` — Execute QEMU monitor commands.
- `POST /nodes/{node}/qemu/{vmid}/move_disk` — Move volume to different storage or to a different VM.
- `POST /nodes/{node}/qemu/{vmid}/snapshot` — Snapshot a VM.
- `POST /nodes/{node}/qemu/{vmid}/snapshot/{snapname}/rollback` — Rollback VM state to specified snapshot.
- `POST /nodes/{node}/qemu/{vmid}/spiceproxy` — Returns a SPICE configuration to connect to the VM.
- `POST /nodes/{node}/qemu/{vmid}/status/reset` — Reset virtual machine.
- `POST /nodes/{node}/qemu/{vmid}/status/resume` — Resume virtual machine.
- `POST /nodes/{node}/qemu/{vmid}/status/shutdown` — Shutdown virtual machine. This is similar to pressing the power button on a physical machine. This will send an ACPI event for the guest OS, which should then proceed to a clean shutdown.
- `POST /nodes/{node}/qemu/{vmid}/status/start` — Start virtual machine.
- `POST /nodes/{node}/qemu/{vmid}/status/stop` — Stop virtual machine. The qemu process will exit immediately. This is akin to pulling the power plug of a running computer and may damage the VM data.
- `POST /nodes/{node}/qemu/{vmid}/status/suspend` — Suspend virtual machine.
- `POST /nodes/{node}/qemu/{vmid}/template` — Create a Template.
- `POST /nodes/{node}/qemu/{vmid}/vncproxy` — Creates a TCP VNC proxy connections.
- `POST /nodes/{node}/services/{service}/reload` — Reload service. Falls back to restart if service cannot be reloaded.
- `POST /nodes/{node}/services/{service}/restart` — Hard restart service. Use reload if you want to reduce interruptions.
- `POST /nodes/{node}/services/{service}/start` — Start service.
- `POST /nodes/{node}/services/{service}/stop` — Stop service.
- `POST /nodes/{node}/spiceshell` — Creates a SPICE shell.
- `POST /nodes/{node}/startall` — Start all VMs and containers located on this node (by default only those with onboot=1).
- `POST /nodes/{node}/status` — Reboot or shutdown a node.
- `POST /nodes/{node}/stopall` — Stop all VMs and Containers.
- `POST /nodes/{node}/storage/{storage}/content` — Allocate disk images.
- `POST /nodes/{node}/storage/{storage}/content/{volume}` — Copy a volume. This is experimental code - do not use.
- `POST /nodes/{node}/storage/{storage}/upload` — Upload templates, ISO images, OVAs and VM images.
- `POST /nodes/{node}/subscription` — Update subscription info.
- `POST /nodes/{node}/vncshell` — Creates a VNC Shell proxy.
- `POST /nodes/{node}/vzdump` — Create backup.
- `POST /pools` — Create new pool.
- `POST /storage` — Create a new storage.
- `PUT /access/acl` — Update Access Control List (add or remove permissions).
- `PUT /access/domains/{realm}` — Update authentication server settings.
- `PUT /access/groups/{groupid}` — Update group data.
- `PUT /access/password` — Change user password.
- `PUT /access/roles/{roleid}` — Update an existing role.
- `PUT /access/users/{userid}` — Update user configuration.
- `PUT /cluster/backup/{id}` — Update vzdump backup job definition.
- `PUT /cluster/firewall/aliases/{name}` — Update IP or Network alias.
- `PUT /cluster/firewall/groups/{group}/{pos}` — Modify rule data.
- `PUT /cluster/firewall/ipset/{name}/{cidr}` — Update IP or Network settings
- `PUT /cluster/firewall/options` — Set Firewall options.
- `PUT /cluster/firewall/rules/{pos}` — Modify rule data.
- `PUT /cluster/ha/groups/{group}` — Update ha group configuration. (deprecated in favor of HA rules)
- `PUT /cluster/ha/resources/{sid}` — Update resource configuration.
- `PUT /cluster/options` — Set datacenter options.
- `PUT /nodes/{node}/dns` — Write DNS settings.
- `PUT /nodes/{node}/firewall/options` — Set Firewall options.
- `PUT /nodes/{node}/firewall/rules/{pos}` — Modify rule data.
- `PUT /nodes/{node}/lxc/{vmid}/config` — Set container options.
- `PUT /nodes/{node}/lxc/{vmid}/firewall/aliases/{name}` — Update IP or Network alias.
- `PUT /nodes/{node}/lxc/{vmid}/firewall/ipset/{name}/{cidr}` — Update IP or Network settings
- `PUT /nodes/{node}/lxc/{vmid}/firewall/options` — Set Firewall options.
- `PUT /nodes/{node}/lxc/{vmid}/firewall/rules/{pos}` — Modify rule data.
- `PUT /nodes/{node}/lxc/{vmid}/resize` — Resize a container mount point.
- `PUT /nodes/{node}/lxc/{vmid}/snapshot/{snapname}/config` — Update snapshot metadata.
- `PUT /nodes/{node}/network/{iface}` — Update network device configuration
- `PUT /nodes/{node}/qemu/{vmid}/config` — Set virtual machine options (synchronous API) - You should consider using the POST method instead for any actions involving hotplug or storage allocation.
- `PUT /nodes/{node}/qemu/{vmid}/firewall/aliases/{name}` — Update IP or Network alias.
- `PUT /nodes/{node}/qemu/{vmid}/firewall/ipset/{name}/{cidr}` — Update IP or Network settings
- `PUT /nodes/{node}/qemu/{vmid}/firewall/options` — Set Firewall options.
- `PUT /nodes/{node}/qemu/{vmid}/firewall/rules/{pos}` — Modify rule data.
- `PUT /nodes/{node}/qemu/{vmid}/resize` — Extend volume size.
- `PUT /nodes/{node}/qemu/{vmid}/sendkey` — Send key event to virtual machine.
- `PUT /nodes/{node}/qemu/{vmid}/snapshot/{snapname}/config` — Update snapshot metadata.
- `PUT /nodes/{node}/qemu/{vmid}/unlink` — Unlink/delete disk images.
- `PUT /nodes/{node}/subscription` — Set subscription key.
- `PUT /nodes/{node}/time` — Set time zone.
- `PUT /pools/{poolid}` — Update pool data (deprecated, no support for nested pools - use 'PUT /pools/?poolid={poolid}' instead).
- `PUT /storage/{storage}` — Update storage configuration.

### Changed endpoints (11)

- `DELETE /nodes/{node}/ceph/pools/{name}`: added `force`
- `POST /access/domains`: added `bind_dn`
- `POST /nodes/{node}/lxc`: changed `mp[n]` (format: ~acl ~backup ~quota ~ro), `net[n]` (format: ~bridge ~firewall ~hwaddr ~mtu ~name ~tag), `rootfs` (format: ~acl ~backup ~quota ~ro); changed `net[n]` (format: ~tag), `rootfs` (format: -backup); changed `mp[n]` (format: ~backup ~mp ~ro), `net[n]` (format: ~tag), `rootfs` (format: ~ro)
- `POST /nodes/{node}/qemu`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `net[n]` (format: "pve-qm-net" → {"bridge":{"description":"Bridge to a…), `numa[n]` (format: ~memory ~policy), `sata[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `scsi[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~queues ~secs ~size ~snapshot ~trans ~werror), `usb[n]` (format: ~usb3), `virtio[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~rerror ~secs ~size ~snapshot ~trans ~werror); changed `net[n]` (format: ~macaddr); changed `net[n]` (format: ~bridge ~link_down ~macaddr ~model ~rate ~tag), `numa[n]` (format: ~cpus ~hostnodes ~memory ~policy), `usb[n]` (format: ~host ~usb3); added `hugepages`
- `POST /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `net[n]` (format: "pve-qm-net" → {"bridge":{"description":"Bridge to a…), `numa[n]` (format: ~memory ~policy), `sata[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `scsi[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~queues ~secs ~size ~snapshot ~trans ~werror), `usb[n]` (format: ~usb3), `virtio[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~rerror ~secs ~size ~snapshot ~trans ~werror); changed `net[n]` (format: ~macaddr); changed `net[n]` (format: ~bridge ~link_down ~macaddr ~model ~rate ~tag), `numa[n]` (format: ~cpus ~hostnodes ~memory ~policy), `usb[n]` (format: ~host ~usb3); added `hugepages`
- `POST /storage`: added `mkdir`, `tagged_only`; changed `monhost` (format: null → "pve-storage-portal-dns-list")
- `PUT /access/domains/{realm}`: added `bind_dn`
- `PUT /cluster/options`: added `mac_prefix`
- `PUT /nodes/{node}/lxc/{vmid}/config`: changed `mp[n]` (format: ~acl ~backup ~quota ~ro), `net[n]` (format: ~bridge ~firewall ~hwaddr ~mtu ~name ~tag), `rootfs` (format: ~acl ~backup ~quota ~ro); changed `net[n]` (format: ~tag), `rootfs` (format: -backup); changed `mp[n]` (format: ~backup ~mp ~ro), `net[n]` (format: ~tag), `rootfs` (format: ~ro)
- `PUT /nodes/{node}/qemu/{vmid}/config`: changed `cpu` (format: ~cputype), `ide[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `net[n]` (format: "pve-qm-net" → {"bridge":{"description":"Bridge to a…), `numa[n]` (format: ~memory ~policy), `sata[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~media ~rerror ~secs ~size ~snapshot ~trans ~werror), `scsi[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~queues ~secs ~size ~snapshot ~trans ~werror), `usb[n]` (format: ~usb3), `virtio[n]` (format: ~aio ~backup ~cache ~cyls ~discard ~format ~heads ~iothread ~media ~rerror ~secs ~size ~snapshot ~trans ~werror); changed `net[n]` (format: ~macaddr); changed `net[n]` (format: ~bridge ~link_down ~macaddr ~model ~rate ~tag), `numa[n]` (format: ~cpus ~hostnodes ~memory ~policy), `usb[n]` (format: ~host ~usb3); added `hugepages`
- `PUT /storage/{storage}`: added `mkdir`, `tagged_only`
